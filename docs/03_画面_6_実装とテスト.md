# 画面（GUI）設計書 ― 実装とテスト

本書は [03_画面.md](03_画面.md) の一部である。章・節の番号は 03_画面.md と分割した各ファイルで通し番号とし、各章がどのファイルにあるかは [03_画面.md](03_画面.md#ファイル構成) の表のとおり。

---

## 12. 実装構成

| ファイル | 内容 |
|---|---|
| `tebunko_grep.bat` | 起動用バッチ。`scripts` 配下の Mark-of-the-Web を消してから、`conhost.exe` を通して `powershell -NoProfile -STA -ExecutionPolicy RemoteSigned -WindowStyle Hidden -File scripts\tebunko_grep\gui.ps1` を `start` で起動する（`conhost.exe` を通すのは、既定のターミナルが Windows Terminal でも窓を隠すため）。ASCII・CRLF で書き、説明コメントは英語にする（cmd はコードページ・改行に敏感なため）。詳細は [03_画面_5_共通仕様.md 10.1](03_画面_5_共通仕様.md#101-配布と実行ポリシーmark-of-the-web) |
| `scripts/tebunko_grep/gui.ps1` | 画面の起動口。Mark-of-the-Web の除去、多重起動の防止（12.1）、ウィンドウとタブの読み込みと `x:Name` の対応表（`$ui`）の作成、各部品の読み込み（**順に意味がある**）、ウィンドウ全体のイベント、`ShowDialog`。判定・検索ロジックは持たない |
| `scripts/tebunko_grep/xaml/tebunko_grep.xaml` | 画面の枠（ウィンドウとタブ）。タブの中身は別ファイルにし、`gui.ps1` が読み込んで入れる |
| `scripts/tebunko_grep/xaml/tab_index.xaml`<br>`tab_search.xaml`<br>`tab_settings.xaml`<br>`tab_kill.xaml` | 各タブの中身。イベントは書かず、`x:Name` だけを付ける。ルート要素には `StaticResource` を使う属性を置かない（自分の `Resources` より先に解決されるため）。`x:Name` が `gui.ps1` の一覧と食い違っていないことは `tests/meta/structure.Tests.ps1` で確かめる |
| `scripts/tebunko_grep/xaml/dialog_index_edit.xaml` | インデックスの追加・編集のダイアログ（[3.2](03_画面_1_インデックス管理タブ.md#32-追加編集のダイアログ)）。追加と編集で同じ定義を使い、表題・説明・注意書きを `ui/index_tab.ps1` で変える |
| `scripts/tebunko_grep/xaml/dialog_indexing_confirm.xaml` | インデックス作成の確認ダイアログ（[3.4](03_画面_1_インデックス管理タブ.md#34-インデックス作成の確認ダイアログ)）。インデックスごとの取り込み対象の件数（`work/取り込み予定.tsv`）を一覧にする。`ui/indexing_tab.ps1` が開く |
| `scripts/shared/xaml/dialog_confirm.xaml` | 確認ダイアログ（[10.2](03_画面_5_共通仕様.md#102-確認ダイアログshowconfirm)）。見出し・結果の一覧（`ItemsControl` に `ConfirmFact` をバインド）・補足だけを定義し、ボタンは場面ごとに違うため `shared/ui/shell.ps1` の `showConfirm` が組み立てて `ButtonPanel` / `ChoicePanel` に入れる |
| `scripts/shared/xaml/theme.xaml` | 画面の見た目（色・文字・コントロールの形）の共通定義 |
| `scripts/shared/ui/types.ps1`<br>`scripts/tebunko_grep/ui/types_grep.ps1` | 画面で使う型（PowerShell class。12.2）。継承元（`NotifyBase`）と共通の型（`ConfirmFact`）は shared に置き、先に読み込む（型の解決は読み込む順に依存するため、`tests/meta/structure.Tests.ps1` で確かめる）。主な型は次のとおり。<br>・`FileGroup`：結果の表の、元のファイル 1 つ分の見出し（[4.9](03_画面_2_検索タブ.md#49-ファイルごとにまとめた表示)）。ヒットは生のまま `Hits` に持ち、表の行は開いたとき・絞り込み・並べ替え・出力のときに作る<br>・`HitRow`：結果の 1 行。件数が多いので**生成時は生データのみ**を持ち、表示用（強調セグメント・DisplayLine・「セル」列）は**画面に出た行だけ** `Prepare()` で作る（`ResultGrid` の `LoadingRow` から呼び、`INotifyPropertyChanged` で反映）。選択行のプレビュー（4.7）の表は `BuildPreview`（`readTsvContext` で読んだ行から `PreviewTable` を作る）で組み立てる<br>・`PreviewColumn`：列の幅。列見出しと各行のセルで共有し、見出しの `Thumb` のドラッグで `SetWidth` を呼ぶと列全体に反映する<br>・`IndexNode`：検索対象のツリー（4.8）。展開時のフォルダ読み込み・3 状態のチェック・検索範囲 `SearchTarget`／除外 `SearchExclude` の組み立てを持つ。チェックは OneWay バインドとし、クリック（`Toggle`）・展開（`LoadChildren`）はイベントで駆動する（PS class はプロパティのセッターにロジックを書けないため） |
| `scripts/shared/ui/app_host.ps1` | 画面の土台。XAML の読み込み（`loadXaml`。`ParserContext.BaseUri` にそのファイルの場所を渡し、`theme.xaml` への相対参照を解決する）、ウィンドウの読み込みとアイコン（`loadWindow`）、色の取得（`themeBrush`）、予期しないエラーの記録（`writeErrorLog`） |
| `scripts/shared/ui/shell.ps1` | 画面の共通部品。ステータス表示（`setStatus`）、メッセージ（`showMessage`）、確認ダイアログ（`showConfirm`。結果の行は `factGone` / `factKept` / `factNext` で `ConfirmFact` を作り、選択肢のボタンは `newChoiceContent` で 2 行のボタンにする。ボタンの `Click` と `ContentRendered` は `GetNewClosure()` で値を取り込んだスクリプトブロックにし、スクリプトスコープの変数に置かない）、例外を拾う `safe`、タイマー（`newTimer`）、別スレッド（`startJob`。12.1） |
| `scripts/shared/ui/folder_dialog.ps1` | Windows 標準のフォルダ選択（`selectFolder`。3.2.1）。WinForms 内部の `IFileDialog` をリフレクションで呼んで開き、開けなければ `FolderBrowserDialog` で選んでもらう（12.2）。フォルダのドラッグ＆ドロップの判定（`getDroppedFolders`・`onFolderDragOver`）も置く |
| `scripts/tebunko_grep/ui/index_tab.ps1`<br>`indexing_tab.ps1` | ［1 インデックス管理］タブ（[03_画面_1](03_画面_1_インデックス管理タブ.md)）。インデックスの一覧・追加・編集・削除と、インデックス作成の起動・中止・進み具合の表示。インデックス一覧のほかでの変更は、`getTargetsKey`（保存されている一覧を比べるための文字列）をウィンドウがアクティブになったときに比べて検出する（8 章） |
| `scripts/tebunko_grep/ui/search_tab.ps1`<br>`result_list.ps1`<br>`preview.ps1`<br>`open_source.ps1`<br>`index_tree.ps1` | ［2 検索］タブ（[03_画面_2](03_画面_2_検索タブ.md)）。検索の実行（`search_tab.ps1`）、結果の表（ファイルごとの見出しと行・絞り込み・並べ替え。`result_list.ps1`）、選択行のプレビュー（`preview.ps1`）、元のファイルを開く処理（Excel COM。[4.5](03_画面_2_検索タブ_1_元のファイルを開く.md#45-元のファイルを開く)。`open_source.ps1`）、検索対象インデックスのツリー（`index_tree.ps1`） |
| `scripts/tebunko_grep/ui/settings_tab.ps1` | ［8 設定］タブ（[03_画面_5 8.1](03_画面_5_共通仕様.md#81-8-設定タブ)）。ワークスペースを変えたら画面を開き直す |
| `scripts/tebunko_grep/ui/process_tab.ps1` | ［9 プロセス停止］タブ（[03_画面_3](03_画面_3_プロセス停止タブ.md)） |
| `scripts/tebunko_grep/ui/index_view.ps1`<br>`indexing_view.ps1`<br>`search_view.ps1`<br>`preview_view.ps1`<br>`settings_view.ps1` | 画面の**判断層**。「何を出すか」を決める部分を `$ui` に触らない関数として切り出したもの（入力は素の値、出力は素の値）。色は意味（`info` / `ok` / `warn` / `ng` / `gray`）で返し、実際の色はタブ側で対応表から引く。Pester でテストする（16.1） |
| `scripts/tebunko_grep/lib.ps1` | 画面とインデックス作成（`tebunko_grep/indexer.ps1`）で使う部品の読み込み口（下表の関数。実体は `core/`・`index/`・`indexer/`・`search/` と `shared/`）。画面が起こす別スレッドもこれを読み込む |

`tebunko_grep/lib.ps1` の関数（画面で使うもの）：

| 関数 | 入力 | 出力 | 概要 | 使用元 |
|---|---|---|---|---|
| `isValidRegex` | pattern | bool | 正規表現として正しいか | 画面・`searchPackIndex` |
| `getIndexPackFiles` | フォルダ、または `@{ Root; RelPath; Recurse }`（検索対象のツリーで選んだ範囲）の配列（既定 `work\index`） | `@{ Folders; Packs }` | 検索対象の集約ファイルを列挙する。Packs は `@{ Path; Root; RelDir; RelPath; Ticks; Size }`（フォルダの順・名前の順、重複なし） | 画面 |
| `testIndexExists` | フォルダの配列 | bool | 集約ファイルが 1 件でもあるか（最初の 1 件で打ち切る。2.2） | 画面 |
| `getIndexSummary` | フォルダの配列 | `@{ Count; LastWrite; Missing }` | 集約ファイルの件数と最新の更新日時 | 画面 |
| `newSearchRegex` | ワード, 文字どおりか, 大文字と小文字を区別するか | `@{ Regex; SimpleMatch }` | 4.2 の検索条件から照合用の正規表現を作る。一致箇所の強調（`HitRow`）にも同じ正規表現を渡す | 画面・`searchPackIndex` |
| `searchPackIndex` | ワード, Packs, 文字どおりか, 上限, 中止の判定, 大文字と小文字を区別するか, 対象ファイル, 並列のスレッド数, 読んだ内容の使い回し（画面が 1 つ持つ `newTsvTextCache`）, 図形・コメントも検索するか, 進捗コールバック | `@{ Hits; SimpleMatch; Total; Truncated; Cancelled }` | 4.1 の検索（照合は `searchPackFiles`＝.NET の `StreamReader`＋`[regex]`）。Hits は PSCustomObject（Root・RelPath（集約ファイル）・RelDir・FileName・Book・Location・LineNumber・Line）。Total は集約ファイルの数 | 画面 |
| `toSearchResultLines` | 結果の配列 | `@{ Header; Lines }` | 検索結果ファイルの見出し行と各行（`toResultLine` / `toResultHeader`） | 画面（コピー）・`writeSearchResult` |
| `writeSearchResult` | writer, ワード, 結果の配列 | – | `【検索文字列　X】 N 件`・見出し行・各行・空行を書き出す | 画面（ファイル出力） |
| `splitTsvCells` | TSV の 1 行 | string[] | セルに分割する（`"` で囲まれたセル内のタブは区切らない。先頭のセルが空でも次のセルを落とさない） | – （画面は同じ処理を `HitRow.SplitCells`（PowerShell class）で行う） |
| `readPackContext` | 集約ファイルのパス, 元のファイル名, 場所, 行番号, 前の行数, 後の行数, 読んだ内容の使い回し（`newTsvTextCache`） | `@{ LineNumber; Line }` の配列 | 4.8 のプレビューに出す行を、集約ファイルの中のその元のファイル・場所から取り出す（検索で読んだ内容があれば読み直さない）。読めない・見つからなければ空 | 画面（選択行のプレビュー） |
| `getSourceLocation` / `resolveSourcePath` | 結果 1 件, フォルダ → 対応 のキャッシュ | `@{Name; Folder; Rest; Known}` / string / `$null` | 元のファイルのインデックス名・場所・パス（4.5.1） | 画面 |
| `findMovedSource` / `setIndexSourceFolder` | 選んだフォルダ, 相対フォルダ, ファイル名 / インデックス名, フォルダ | `@{Path; Root}` / `$null` | 元のファイルが見つからないとき、選んだフォルダから探し、インデックス名に対する元のフォルダを設定に記録する（4.5.2） | 画面 |
| `getTargetFolders` / `writeTargetFolders` / `normalizeFolderPath` | – / `@{ Name; Path; Enabled }` の配列 / パス | 同上 / – / パス | インデックス一覧（`setting.config` の `targetFolders`。インデックス名と置き場所を分けて持つ）。［編集…］は Name・Path を書き換える（[03_画面_1](03_画面_1_インデックス管理タブ.md)） | 画面・インデックス作成 |
| `readSearchOption` / `writeSearchOption` | – / 変える項目の hashtable | `@{ UseRegex; CaseSensitive; FileFilter; IncludeShapes; IncludeComments }` | `setting.config` の `useRegex` / `caseSensitive` / `fileFilter` / `includeShapes` / `includeComments`（8 章） | 画面 |
| `readOpenMode` / `writeOpenMode` | – / 開き方 | 開き方（`normal` / `readOnly` / `new`。知らない値は `normal`） | `setting.config` の `openMode`（8 章、[4.5](03_画面_2_検索タブ_1_元のファイルを開く.md#45-元のファイルを開く)） | 画面 |
| `getSearchIndexes` | – | `@{ Name; Path; SourcePath }` の配列 | インデックスの一覧（`work\index` 直下のフォルダ）。検索対象のツリーの一番上の項目に使う（4.8） | 画面 |
| `readSearchExcludes` / `writeSearchExcludes` | – / `@{ Path; Subfolders }` の配列 | 同左 / – | `setting.config` の `searchExcludes`（検索対象のツリーでチェックを外したフォルダ、4.8）を読み書きする | 画面 |
| `getIndexingState` | – | `@{ Exists; Folders; Total; Pending; Failed; Done; ...; FailedRows }` | 6.1 の中断・失敗の件数、失敗したファイルと原因の一覧（`readStatusFile`） | 画面 |
| `getOfficeProcesses` | – | 種類・PID・バックグラウンドか・起動時刻・メモリ・タイトル の配列 | 5.1 の一覧 | 画面 |
| `stopOfficeProcesses` | PID の配列 | `@{ Id; Stopped; Message }` の配列 | `Stop-Process -Force` | 画面 |

### 12.1 待たせ方の方針（画面を固まらせない）

**画面が「応答なし」になることは、処理が長いこと自体よりも利用者を不安にさせる**（壊れたと思って強制終了され、インデックス作成が中断して Office のプロセスが残る）。そのため、次を守る。

| 方針 | 具体 |
|---|---|
| **画面のスレッドで 0.2 秒を超える可能性のある処理をしない** | ファイル数・行数に比例する処理（検索、取り込み一覧の集計、インデックスの削除、TSV の数え上げ）は別スレッド（Runspace）で行い、終わったら画面のスレッドで結果を反映する。終わるまでは、その操作のボタンを無効にして何をしているかをステータスに出す |
| **繰り返し読むものは小さくする** | 1 秒ごとに読むものは「進み具合の 1 行」（`work/インデックス作成進捗.txt`。実測 約 1 ミリ秒）にする。数万行の取り込み一覧を毎秒読み直すと、その間ずっと画面が止まる（5 万行で 1 回約 1.4 秒）。書く側（インデクサ）が数えて 1 行にする |
| **無言の時間を作らない** | 待ちが数秒を超えうる場所では「何をしているか」を必ず出す（`クロールしています…` ＋ 今見ているフォルダ、`インデックス作成を終えています…` ＋ 今の後片付け、`検索対象のファイルを確認しています…（N 件）`、`インデックス […] の TSV を削除しています…`）。件数が増えるのが見えれば、止まっていないと分かる |
| **長い処理は止められるようにする** | インデックス作成は［中止］（ファイルの切れ目で止まる・続きから再開できる）、検索は［中止］（それまでの結果は残す） |
| **終わったことを知らせる** | インデックス作成が終わったとき、進み具合とステータスで知らせる（タスクバーのボタンを光らせる `FlashWindowEx` は P/Invoke が要るため使わない。12.2） |

数万件規模での実測（この方針の根拠。Windows PowerShell 5.1・ローカルディスク。1 万 = 元ファイル 1 万件（取り込み一覧 1 万行・TSV 2 万件）、5 万 = 取り込み一覧 5 万行）:

| 処理 | 1 万 | 5 万 | 扱い |
|---|---|---|---|
| 取り込み一覧の読み込み（`readStatusFile`） | 0.32 秒 | 1.4 秒 | 1 行ごとに関数を呼ばない。毎秒は読まず、進み具合の 1 行を読む |
| 取り込み一覧の書き出し（`writeStatusFile`） | 0.08 秒 | 0.22 秒 | 1 行ごとに関数・パイプラインを使わない（インデックス作成の開始・終了時の待ちに効く） |
| 状態の集計（`getIndexingState`） | 0.91 秒 | 5.3 秒 | 別スレッドで行う（`refreshIndexingState`）。日時の解析は必要なときだけ行う |
| 検索対象の数え上げ（集約ファイルにする前の TSV の列挙） | 2.2 秒（TSV 2 万件） | – | .NET で列挙する。検索スレッドで行い、途中の件数を表示する。今は集約ファイル（フォルダ・拡張子ごとに 1 つ）だけを列挙する（`getIndexPackFiles`） |
| インデックスの削除 | 6.7 秒（1 万フォルダ） | – | 別スレッドで行う（削除中は操作を止めてステータスに出す） |

> PowerShell 5.1 では**関数呼び出しが 1 回あたり約 50 マイクロ秒**かかる。5 万行で 1 行 1 回呼ぶと、それだけで約 2.7 秒になる。数万件を回すところでは、関数呼び出し・パイプライン（`ForEach-Object` / `Where-Object`）・`$obj.$名前` の動的アクセスを避ける（普通の場所では読みやすさを優先してよい）。

別スレッドとタイマーの扱い：

- 検索は `search_tab.ps1` が専用の Runspace（`[powershell]::Create()`）で実行する。結果はスレッド間で共有するキュー（`ConcurrentQueue`）に入れ、画面の `DispatcherTimer`（0.1 秒間隔）で取り出して表に加える。1 回に取り出す量は件数ではなく時間で区切る（件数で区切ると、ヒットが多いときに画面が止まる）。中止はフラグで伝える。
- 集約ファイルの件数取得・取り込み一覧の集計・インデックスの削除は `startJob`（`shell.ps1`）で実行する。終わったかは `DispatcherTimer`（0.2 秒間隔）で確かめ、画面のスレッドで `onDone` を呼ぶ。同じ集計が重ならないよう、実行中なら「もう一度」の印だけを立てて、終わってからやり直す。
- インデックス作成のプロセスは `Start-Process -PassThru`（`-WindowStyle Hidden`）で起動し、`DispatcherTimer`（1 秒間隔）で `HasExited` と進み具合の 1 行（`work/インデックス作成進捗.txt`）を確認する（3.6）。終了後は `ExitCode` で表示を分ける（3.7）。起動時と開始時は、`Win32_Process` のコマンドラインから実行中のインデックス作成を探し、見つかればそのプロセスを監視する（3.3）。
- 進み具合の段階が `確認` になったら、インデックス作成の確認ダイアログを開く（3.4）。**ダイアログを開いている間も `DispatcherTimer` は動く**（`ShowDialog` は入れ子のメッセージループのため）。開く前に「開いた」ことにして、二重に開かないようにする。
- 多重起動は、ツールの配置フォルダごとの名前付き Mutex で防ぐ。2 つ目のプロセスは、同じくフォルダごとの名前付きイベント（`Local\tebunko_grep_gui_activate_<フォルダのハッシュ>`）を合図して終了し、すでに開いている画面が `DispatcherTimer`（0.3 秒間隔）でそれを確認して `Window.Activate()`（＋最小化なら復帰）で前面に出す。別アプリが前面のときはフォーカスを奪えないことがある（OS の制限。P/Invoke の `SetForegroundWindow` は使わない。12.2）。

注意（PowerShell 5.1）：`System.Collections.Generic.List[object]` を `@()` で配列にする・`[object[]]` 引数に渡すと例外（Argument types do not match）になる。結果のリストは `List[psobject]` にするか `.ToArray()` で返す。また PS class のメソッド内では、クラスのプロパティと同じ名前（大文字小文字を区別しない）のローカル変数は使えない（`$this.` の付け忘れとみなされる）ため、別名にする。

### 12.2 実行時コンパイル（csc.exe）を使わない

資産管理・EDR は「`powershell.exe` が `csc.exe` を起動して `%TEMP%` の一時 DLL を読み込む」動き（実行時コンパイル。MITRE ATT&CK T1027.004）を、良性でも拾うことがある。tebunko_grep はこれを一切出さない設計にする（`tests/meta/structure.Tests.ps1` の「実行時コンパイル（csc.exe）を使わない」と `tests/meta/safety.Tests.ps1` が確かめる。[04_安全性.md 2.1](04_安全性.md#21-検査項目と結果)）。

- **画面で使う型は PowerShell class**（`HitRow`・`FileGroup`・`Segment`・`PreviewColumn`／`PreviewCell`／`PreviewRow`／`PreviewTable`・`ProcRow`・`FailRow`・`PlanRow`・`FolderItem`・`SearchTarget`・`SearchExclude`・`IndexNode`・`ConfirmFact`）。PS class はエンジンがメモリ内で用意し、`csc.exe`・一時 DLL を出さない。`INotifyPropertyChanged` は `NotifyBase` を継承して実装する。
- **検索・集約ファイルの読み取りは .NET を直接呼ぶ**（`searchPackFiles`／`readPackPlaces`／`readPackContext`。`StreamReader`＋`[regex]`）。
- **Win32 API（P/Invoke）と、インターフェース定義が要る COM を使わない**。代わりに次を使う。

  | 目的 | 使うもの | 使わないもの（実行時コンパイルが要る） |
  |---|---|---|
  | フォルダ選択 | Windows 標準のエクスプローラー形式のダイアログ（`IFileOpenDialog`）。インターフェースの定義は WinForms が内部に持つもの（`FileDialogNative+IFileDialog`）をリフレクションで使う（[3.2.1](03_画面_1_インデックス管理タブ.md#321-フォルダ選択ダイアログ参照)） | `IFileOpenDialog` のインターフェースを自分で定義すること |
  | ドライブの列挙・ネットワークドライブの割り当て先 | `System.IO.DriveInfo`・CIM（`Win32_LogicalDisk`） | `QueryDosDevice` |
  | 多重起動時の前面化 | `Window.Activate` | `SetForegroundWindow`／`AttachThreadInput` |
  | 完了の通知 | 進み具合とステータスの表示 | `FlashWindowEx` |
  | タスクバー・タイトルバーのアイコン | `Window.Icon` | `SetCurrentProcessExplicitAppUserModelID` |

- 結果、起動〜検索を通じて `Add-Type`（実行時コンパイル）は 0 回、`csc.exe` の起動・一時 DLL の生成も 0（実起動で確認）。読み込むのは Microsoft 署名済みの GAC アセンブリ（WPF・WinForms）だけ。
- 代償と対処：PS class はメソッド呼び出しが C# より遅い（実測: 検索コアは C# の約 2.6 倍だが 50 万行で約 0.6 秒。結果行 `HitRow` の一括生成は 1 万件で約 10 秒と遅いため、**可視行だけ作る遅延描画**（`HitRow.Prepare`＋`LoadingRow`。軽量生成は 1 万件で約 0.56 秒）で回避する）。PS class はプロパティのセッターにロジックを書けないため、列幅・チェック・選択は `Set*` メソッドやイベントで変える。`subst` ドライブは実体のパスに解決しない。タスクバーのボタンは PowerShell と同じグループにまとめられる（アイコン自体は tebunko_grep のものが出る。[10 章](03_画面_5_共通仕様.md#10-表示アクセシビリティ)）。

---

## 13. 既存スクリプトへの影響と段階

| 段階 | 内容 | 状態 |
|---|---|---|
| 第 1 段階 | 3 タブ構成、インデックス作成の起動と状態表示、画面内での検索（結果の表・強調・並べ替え・絞り込み・選択行の詳細・元のファイルを開く・コピー・ファイル出力・正規表現の切り替え）、インデックス作成の進み具合、プロセス停止 | **実装済み** |
| 第 2 段階 | 画面だけで操作する。インデックス作成はウィンドウなしで起動し、［中止］・終了後の表示・［ログを開く］・インデックス作成中に閉じるときの確認（3.7）。実行中のインデックス作成の検出（3.1）。［インデックス作成を開始］で取り込み対象を数えて、インデックスごとの件数を確認する（3.4。失敗分の再取り込みもここで選ぶ）。画面とインデクサの受け渡しは、`tebunko_grep/indexer.ps1` の引数（`-ConfirmTargets`・`-RetryFailed`）・終了コード（0 / 1 / 2）と `work/` のファイル（`取り込み予定.tsv`・`インデックス作成開始要求`・`インデックス作成中止要求`・`インデックス作成エラー.txt`・`インデックス作成ログ.txt`）で行う | **実装済み** |
| 第 3 段階 | Word・PowerPoint の該当ページ・スライドへの移動（Word・PowerPoint の COM 操作を追加する） | 検討 |

---

## 14. 未決事項

いずれも第 1 段階の実装で決めた。

| # | 事項 | 選択肢 | 決定 |
|---|---|---|---|
| U1 | 入口の名前 | `0_設定.bat` / `tebunko_grep.bat` / `0_tebunko_grep.bat` など | **`tebunko_grep.bat`**。画面がすべての操作の入口で `.bat` もこれ 1 本だけのため、番号を付けず画面の表題（`tebunko_grep`）と同じ名前にする |
| U2 | 正規表現の既定 | オン / オフ（文字どおり） | **オフ** |
| U3 | 検索結果の件数の上限 | 1,000 / 10,000 / 上限なし | **10,000 件** |
| U4 | インデックス作成中の検索 | 許可（注意を表示） / 禁止 | **許可**（注意を表示） |
| U5 | 複数ワードの一括検索 | 設ける / 設けない | **設けない** |
| U6 | 元のファイルを開くときの Excel | 起動中の Excel を使う / 常に新しく起動する / 既定のアプリで開くだけ | **起動中の Excel を使う**（無ければ起動）。該当セルを選択できる |
| U7 | ファイル出力の対象 | 絞り込み後の表示分 / 全件 | **絞り込み後の表示分** |

---

## 15. 既知の制約

確度: ◎ = 動作確認済み、○ = コード上明らか、△ = 推定。

| # | 内容 | 確度 |
|---|---|---|
| 1 | 元のファイルのパスは、取り込み時に記録したクロール対象フォルダから組み立てる。取り込み後に元のフォルダを移動・名前変更した場合は、開くときにフォルダを選び直す必要がある（選んだフォルダは置き換えとして記録し、同じフォルダの下は次から聞かない。4.5.2） | ○ |
| 2 | `元のフォルダ.txt` の無いインデックス（インデックス作成を 1 回実行すると作られる）を別の場所にコピーした場合は、元の場所が分からないため、開くときにフォルダを選ぶ必要がある | ○ |
| 3 | バックグラウンドの判定は `MainWindowHandle` による。画面に表示中でも、すべてのウィンドウを閉じた直後などはバックグラウンドと判定される（5.1） | △ |
| 4 | フォルダ選択は Windows 標準のダイアログのため、フォルダの中のファイル（Office ファイルがあるか）は一覧に出ない（[3.2.1](03_画面_1_インデックス管理タブ.md#321-フォルダ選択ダイアログ参照)）。WinForms の内部の型を使うため、Windows PowerShell 5.1（.NET Framework）が前提。内部の型が使えないときはツリー形式の `FolderBrowserDialog` になる | ○ |
| 5 | 集約ファイルが非常に多い場合、件数を数え終わるまで状態表示が `確認中…` になる | △ |

---

> **16. テスト**（16.1 単体テスト・16.2 画面の確認）→ [03_画面_6_実装とテスト_1_テスト.md](03_画面_6_実装とテスト_1_テスト.md#16-テスト)
