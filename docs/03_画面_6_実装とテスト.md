# 画面（GUI）設計書 ― 実装とテスト

本書は [03_画面.md](03_画面.md) の一部である。章・節の番号は 03_画面.md と分割した各ファイルで通し番号とし、各章がどのファイルにあるかは [03_画面.md](03_画面.md#ファイル構成) の表のとおり。

---

## 12. 実装構成

| ファイル | 内容 |
|---|---|
| `win_grep.bat` | ①フォルダの Mark-of-the-Web を消す（`powershell -NoProfile -Command "Get-ChildItem -LiteralPath '%~dp0' -Recurse -File \| Unblock-File"`）②`start "" powershell -NoProfile -STA -ExecutionPolicy RemoteSigned -WindowStyle Hidden -File "%~dp0scripts\config_gui.ps1"`。ASCII・CRLF で書く（cmd はコードページ・改行に敏感なため。説明コメントは英語）。詳細は [03_画面_5_共通仕様.md 10.1](03_画面_5_共通仕様.md#101-配布と実行ポリシーmark-of-the-web) |
| `scripts/config_gui.xaml` | 画面定義（3 タブ・インデックス一覧の表・結果の表・プロセスの表）。イベントは書かず、`x:Name` だけを付ける |
| `scripts/config_gui_index_edit.xaml` | インデックスの新規作成・編集のダイアログ（[3.2](03_画面_1_インデックス管理タブ.md#32-新規作成編集のダイアログ)）。新規作成と編集で同じ定義を使い、表題・説明・注意書きを `config_gui.ps1` で変える |
| `scripts/config_gui_convert_confirm.xaml` | 変換の確認ダイアログ（[3.4](03_画面_1_インデックス管理タブ.md#34-変換の確認ダイアログ)）。インデックスごとの変換対象の件数（`work/変換予定.tsv`）を一覧にして、変換するかどうかを選ぶ |
| `scripts/config_gui.ps1` | 画面の読み込み、イベント処理、別スレッドの管理、変換の起動と進み具合の表示、元のファイルを開く処理（Excel COM）、フォルダ選択（.NET Framework 標準の `FolderBrowserDialog`。パスの貼り付けは入力欄側で受ける）。判定・検索ロジックは持たず、`common.ps1` の関数を呼ぶ。インデックス一覧のほかでの変更は、`getTargetsKey`（保存されている一覧を比べるための文字列）をウィンドウがアクティブになったときに比べて検出する（8 章）。**画面で使う型は PowerShell class で定義する**（実行時コンパイル `Add-Type`＝csc.exe を出さないため。12.2 参照）。結果の1行 `HitRow` は件数が多いので**生成時は生データのみ**を持ち、表示用（強調セグメント・DisplayLine・「セル」列）は**画面に出た行だけ** `HitRow.Prepare()` で作る（`ResultGrid` の `LoadingRow` から呼び、`INotifyPropertyChanged` で反映）。選択行のプレビュー（4.7）の表は `HitRow.BuildPreview`（`readTsvContext` で読んだ行から `PreviewTable` を作る）で組み立てる。列の幅は `PreviewColumn`（`INotifyPropertyChanged`）を列見出しと各行のセルで共有し、見出しの `Thumb` のドラッグで `SetWidth` を呼ぶと列全体に反映する。検索対象のツリー（4.8）は `IndexNode`（展開時のフォルダ読み込み・3 状態のチェック・検索範囲 `SearchTarget`／除外 `SearchExclude` の組み立てを持つ）で表し、`TreeView` にバインドする。チェックは OneWay バインドとし、クリック（`Toggle`）・展開（`LoadChildren`）はイベントで駆動する（PS class はプロパティのセッターにロジックを書けないため） |
| `scripts/common.ps1` | 画面と変換（`office_to_tsv.ps1`）で使う関数（下表）。Pester でテストする |

`common.ps1` の関数（画面で使うもの）：

| 関数 | 入力 | 出力 | 概要 | 使用元 |
|---|---|---|---|---|
| `isValidRegex` | pattern | bool | 正規表現として正しいか | 画面・`searchIndex` |
| `getIndexTsvFiles` | フォルダ、または `@{ Root; RelPath; Recurse }`（検索対象のツリーで選んだ範囲）の配列（既定 `work\index`） | `@{ Folders; Files }` | 検索対象の TSV を列挙する。Files は TSV のフルパス → `@{ Root; RelPath }`（パス順、重複なし） | 画面 |
| `testIndexExists` | フォルダの配列 | bool | TSV が 1 件でもあるか（最初の 1 件で打ち切る。2.2） | 画面 |
| `getIndexSummary` | フォルダの配列 | `@{ Count; LastWrite; Missing }` | TSV の件数と最新の更新日時 | 画面 |
| `newSearchRegex` | ワード, 文字どおりか, 大文字と小文字を区別するか | `@{ Regex; SimpleMatch }` | 4.2 の検索条件から照合用の正規表現を作る。一致箇所の強調（`HitRow`）にも同じ正規表現を渡す | 画面・`searchIndex` |
| `searchIndex` | ワード, Files, 文字どおりか, 上限, 何ファイルずつか, 進捗コールバック, 中止の判定, 大文字と小文字を区別するか, 対象ファイル | `@{ Hits; SimpleMatch; Total; Truncated; Cancelled }` | 4.1 の検索（照合は `searchTsvFiles`＝.NET の `StreamReader`＋`[regex]`）。Hits は PSCustomObject（Root・RelPath・RelDir・FileName・Book・Location・LineNumber・Line）。Total は対象ファイルで絞った後の TSV の数 | 画面 |
| `toSearchResultLines` | 結果の配列 | `@{ Header; Lines }` | 検索結果ファイルの見出し行と各行（`toResultLine` / `toResultHeader`） | 画面（コピー）・`writeSearchResult` |
| `writeSearchResult` | writer, ワード, 結果の配列 | – | `【検索文字列　X】 N 件`・見出し行・各行・空行を書き出す | 画面（ファイル出力） |
| `splitTsvCells` | TSV の 1 行 | string[] | セルに分割する（`"` で囲まれたセル内のタブは区切らない。先頭のセルが空でも次のセルを落とさない） | – （画面は同じ処理を `HitRow.SplitCells`（PowerShell class）で行う） |
| `readTsvContext` | TSV のパス, 行番号, 前の行数, 後の行数 | `@{ LineNumber; Line }` の配列 | 4.8 のプレビューに出す行を読む。PowerShell（`StreamReader`）で先頭から目的の行まで読む（TSV は元のファイル1つ分＝通常は数千行までのため十分）。読めなければ空 | 画面（選択行のプレビュー） |
| `getSourceLocation` / `resolveSourcePath` | 結果 1 件, フォルダ → 対応 のキャッシュ | `@{Name; Folder; Rest; Known}` / string / `$null` | 元のファイルのインデックス名・場所・パス（4.5.1） | 画面 |
| `findMovedSource` / `setIndexSourceFolder` | 選んだフォルダ, 相対フォルダ, ファイル名 / インデックス名, フォルダ | `@{Path; Root}` / `$null` | 元のファイルが見つからないとき、選んだフォルダから探し、インデックス名に対する元のフォルダを設定に記録する（4.5.2） | 画面 |
| `getTargetFolders` / `writeTargetFolders` | – / `@{Name; Path; Enabled}` の配列 | 同上 / – | インデックス一覧（インデックス名と置き場所を分けて持つ）。［編集…］は Name・Path を書き換える（[03_画面_1](03_画面_1_インデックス管理タブ.md)） | 画面 |
| `readSearchOption` / `writeSearchOption` | – / 変える項目の hashtable | `@{ UseRegex; CaseSensitive; FileFilter }` | `setting.config` の `useRegex` / `caseSensitive` / `fileFilter`（8 章） | 画面 |
| `readOpenMode` / `writeOpenMode` | – / 開き方 | 開き方（`normal` / `readOnly` / `new`。知らない値は `normal`） | `setting.config` の `openMode`（8 章、[4.5](03_画面_2_検索タブ_1_元のファイルを開く.md#45-元のファイルを開く)） | 画面 |
| `getSearchIndexes` | – | `@{ Name; Path; SourcePath }` の配列 | インデックスの一覧（`work\index` 直下のフォルダ）。検索対象のツリーの一番上の項目に使う（4.8） | 画面 |
| `readSearchExcludes` / `writeSearchExcludes` | – / `@{ Path; Subfolders }` の配列 | 同左 / – | `setting.config` の `searchExcludes`（検索対象のツリーでチェックを外したフォルダ、4.8）を読み書きする | 画面 |
| `getTargetFolders` / `writeTargetFolders` / `normalizeFolderPath` | – / `@{ Path; Enabled }` の配列 / パス | – | 変換対象フォルダ（`setting.config` の `targetFolders`）の読み書き（3 章） | 画面・変換 |
| `getConversionState` | – | `@{ Exists; Folders; Total; Pending; Failed; Done; ...; FailedRows }` | 6.1 の中断・失敗の件数、失敗したファイルと原因の一覧（`readStatusFile`） | 画面 |
| `getOfficeProcesses` | – | 種類・PID・バックグラウンドか・起動時刻・メモリ・タイトル の配列 | 5.1 の一覧 | 画面 |
| `stopOfficeProcesses` | PID の配列 | `@{ Id; Stopped; Message }` の配列 | `Stop-Process -Force` | 画面 |

### 12.1 待たせ方の方針（画面を固まらせない）

**画面が「応答なし」になることは、処理が長いこと自体よりも利用者を不安にさせる**（壊れたと思って強制終了され、変換が中断して Office のプロセスが残る）。そのため、次を守る。

| 方針 | 具体 |
|---|---|
| **画面のスレッドで 0.2 秒を超える可能性のある処理をしない** | ファイル数・行数に比例する処理（変換一覧の集計、インデックスの削除、TSV の数え上げ）は Runspace（`startJob`）で行い、終わったら画面のスレッドで結果を反映する。終わるまでは、その操作のボタンを無効にして何をしているかをステータスに出す |
| **繰り返し読むものは小さくする** | 1 秒ごとに読むものは「進み具合の 1 行」（`work/変換進捗.txt`）にする。数万行の変換一覧を毎秒読み直すと、その間ずっと画面が止まる（5 万行で 1 回約 5.8 秒）。書く側（変換）が数えて 1 行にする |
| **無言の時間を作らない** | 待ちが数秒を超えうる場所では「何をしているか」を必ず出す（`変換対象のファイルを確認しています…` ＋ 今見ているフォルダ、`変換を終えています…` ＋ 今の後片付け、`検索対象のファイルを確認しています…（N 件）`、`インデックス […] の TSV を削除しています…`）。件数が増えるのが見えれば、止まっていないと分かる |
| **長い処理は止められるようにする** | 変換は［中止］（ファイルの切れ目で止まる・続きから再開できる）、検索は［中止］（それまでの結果は残す） |
| **終わったことを知らせる** | 変換が終わったとき、進み具合とステータスで知らせる（以前はタスクバーのボタンを光らせていたが、P/Invoke（`FlashWindowEx`）を無くすため廃止した） |

数万件規模での実測（この方針の根拠。1 万ファイル ≒ TSV 2 万件 / 変換一覧 1 万行、5 万ファイルは 5 万行）:

| 処理 | 1 万 | 5 万 | 対応 |
|---|---|---|---|
| 変換一覧の読み込み（`readStatusFile`） | 1.2 秒 → 0.32 秒 | 5.8 秒 → 1.4 秒 | 1 行ごとの関数呼び出しをやめた。**毎秒の読み直しも廃止**（進み具合の 1 行を読む。実測 約 1 ミリ秒） |
| 変換一覧の書き出し（`writeStatusFile`） | 3.1 秒 → 0.08 秒 | 17.0 秒 → 0.22 秒 | 1 行ごとの関数呼び出し・パイプラインをやめた（変換の開始・終了時の無言の待ちが短くなる） |
| 状態の集計（`getConversionState`） | 2.3 秒 → 0.91 秒 | 15.5 秒 → 5.3 秒 | 別スレッドへ（`refreshConversionState`）。日時の解析も、必要なときだけ行う |
| 検索対象の数え上げ（`getIndexTsvFiles`） | 6.0 秒 → 2.2 秒（TSV 2 万件） | – | .NET の列挙に変更。検索スレッドで行い、途中の件数を表示する |
| インデックスの削除 | 6.7 秒（1 万フォルダ） | – | 別スレッドへ（削除中は操作を止めてステータスに出す） |

（測定: Windows PowerShell 5.1・ローカルディスク。1 万 = 元ファイル 1 万件（変換一覧 1 万行・TSV 2 万件）、5 万 = 変換一覧 5 万行）

> PowerShell 5.1 では**関数呼び出しが 1 回あたり約 50 マイクロ秒**かかる。5 万行で 1 行 1 回呼ぶと、それだけで約 2.7 秒になる。数万件を回すところでは、関数呼び出し・パイプライン（`ForEach-Object` / `Where-Object`）・`$obj.$名前` の動的アクセスを避ける（普通の場所では読みやすさを優先してよい）。

別スレッドの扱い：

- 検索・TSV の件数取得・変換一覧の集計・インデックスの削除は Runspace（`startJob`）で実行する。検索結果はスレッド間で共有するキュー（`ConcurrentQueue`）に入れ、画面の `DispatcherTimer`（0.1 秒間隔）で取り出して表に加える。中止はフラグで伝える。同じ集計が重ならないよう、実行中なら「もう一度」の印だけを立てて、終わってからやり直す。
- 変換のプロセスは `Start-Process -PassThru`（ウィンドウなし）で起動し、`DispatcherTimer`（1 秒間隔）で `HasExited` と進み具合の 1 行（`work/変換進捗.txt`）を確認する（3.6）。終了後は `ExitCode` で表示を分ける（3.7）。起動時と開始時は、`Win32_Process` のコマンドラインから実行中の変換を探し、見つかればそのプロセスを監視する（3.3）。
- 進み具合の段階が `確認` になったら、変換の確認ダイアログを開く（3.4）。**ダイアログを開いている間も `DispatcherTimer` は動く**（`ShowDialog` は入れ子のメッセージループのため）。開く前に「開いた」ことにして、二重に開かないようにする。
- 多重起動は、ツールの配置フォルダごとの名前付き Mutex で防ぐ。2 つ目のプロセスは、同じくフォルダごとの名前付きイベント（`Local\win_grep_gui_activate_<フォルダのハッシュ>`）を合図して終了し、すでに開いている画面が `DispatcherTimer`（0.3 秒間隔）でそれを確認して `Window.Activate()`（＋最小化なら復帰）で前面に出す。※以前は C# の P/Invoke（`AttachThreadInput`＋`SetForegroundWindow`）で確実に前面を奪っていたが、実行時コンパイルを無くすため廃止した。別アプリが前面のときはフォーカスを奪えないことがある（OS の制限）。

注意（PowerShell 5.1）：`System.Collections.Generic.List[object]` を `@()` で配列にする・`[object[]]` 引数に渡すと例外（Argument types do not match）になる。結果のリストは `List[psobject]` にするか `.ToArray()` で返す。また PS class のメソッド内では、クラスのプロパティと同じ名前（大文字小文字を区別しない）のローカル変数は使えない（`$this.` の付け忘れとみなされる）ため、別名にする。

### 12.2 実行時コンパイル（csc.exe）を使わない

資産管理・EDR は「`powershell.exe` が `csc.exe` を起動して `%TEMP%` の一時 DLL を読み込む」動き（実行時コンパイル。MITRE ATT&CK T1027.004）を、良性でも拾うことがある。win_grep はこれを一切出さない設計にする。

- **画面で使う型は PowerShell class**（`HitRow`・`Segment`・`PreviewColumn`／`Cell`／`Row`／`Table`・`ProcRow`・`FailRow`・`PlanRow`・`FolderItem`・`SearchTarget`・`SearchExclude`・`IndexNode`）。PS class はエンジンがメモリ内で用意し、`csc.exe`・一時 DLL を出さない。`INotifyPropertyChanged` は `NotifyBase` を継承して実装する。
- **検索・TSV 読み取りは .NET を直接呼ぶ**（`common.ps1` の `searchTsvFiles`／`newTsvFiles`／`readTsvContext`。`StreamReader`＋`[regex]`）。
- **Win32/COM 連携（P/Invoke）を使わない**。フォルダ選択は WinForms の `FolderBrowserDialog`、ドライブ解決は CIM（`Win32_LogicalDisk`）、多重起動の前面化は `Window.Activate`、タスクバー・タイトルバーのアイコンは `Window.Icon`。以前使っていた `IFileOpenDialog`・`QueryDosDevice`・`SetForegroundWindow`／`AttachThreadInput`・`FlashWindowEx`・`SetCurrentProcessExplicitAppUserModelID` は廃止した。
- 結果、起動〜検索を通じて `Add-Type`（実行時コンパイル）は 0 回、`csc.exe` の起動・一時 DLL の生成も 0（実起動で確認）。読み込むのは Microsoft 署名済みの GAC アセンブリ（WPF・WinForms）だけ。
- 代償と対処：PS class はメソッド呼び出しが C# より遅い（実測: 検索コアは C# の約 2.6 倍だが 50 万行で約 0.6 秒。結果行 `HitRow` の一括生成は 1 万件で約 10 秒と遅いため、**可視行だけ作る遅延描画**（`HitRow.Prepare`＋`LoadingRow`。軽量生成は 1 万件で約 0.56 秒）で回避する）。また PS class はプロパティのセッターにロジックを書けないため、列幅・チェック・選択は `Set*` メソッドやイベントで変える。フォルダ選択でパスを貼り付ける操作と、`subst` ドライブの解決は使えなくなる。

---

## 13. 既存スクリプトへの影響と段階

| 段階 | 内容 | 既存スクリプトへの影響 |
|---|---|---|
| 第 1 段階（**実装済み**） | 3 タブ構成、変換の起動と状態表示、画面内での検索（結果の表・強調・並べ替え・絞り込み・選択行の詳細・元のファイルを開く・コピー・ファイル出力・正規表現の切り替え）、変換の進み具合、プロセス停止 | `grep.ps1`・`kill_process.ps1` の処理を `common.ps1` の関数に切り出した（動作は変えていない。両スクリプトは第 2 段階で削除した） |
| 第 2 段階（**実装済み**） | 画面だけで操作する。変換はウィンドウなしで起動し、［中止］・終了後の表示・［ログを開く］・変換中に閉じるときの確認（3.7）。実行中の変換の検出（3.1） | `office_to_tsv.ps1`：`-RetryFailed` 引数、中止の要求（`work/変換中止要求`）、終了コード（0 / 1 / 2）、`work/変換エラー.txt`・`work/変換ログ.txt` を追加し、y/N・`pause` をやめた。`1_変換.bat`・`2_検索.bat`・`9_Office強制終了.bat`・`grep.ps1`・`kill_process.ps1`・`config/検索ワード.txt` を廃止した |
| 第 2 段階の追加（**実装済み**） | ［変換を開始］で、まず変換対象を数えてインデックスごとの件数を確認する（3.4）。失敗分の再変換もこの確認で選ぶ。確認できるため、インデックスを一から作り直す［作り直す…］ボタンは無くした | `office_to_tsv.ps1`：`-ConfirmTargets` 引数と、画面との受け渡し（`work/変換予定.tsv`・`work/変換開始要求`）、進み具合の段階 `確認` を追加した |
| 第 3 段階（検討） | Word・PowerPoint の該当ページ・スライドへの移動（既定以外のインデックスフォルダの結果を開くことは、`元のフォルダ.txt` と置き換えで対応済み。4.5.1・4.5.2） | Word・PowerPoint の COM 操作を追加する |

---

## 14. 未決事項

| # | 事項 | 選択肢 | 推奨（第 1 段階の実装はこのとおり） |
|---|---|---|---|
| U1 | 入口の名前 | `0_設定.bat` / `win_grep.bat` / `0_win_grep.bat` など | **`win_grep.bat` に決定**（決定済み）。画面がすべての操作の入口になり、`.bat` もこれ 1 本だけになったため、番号を付けず画面の表題（`win_grep`）と同じ名前にした |
| U2 | 正規表現の既定 | オン（以前の `grep.ps1` の動作） / オフ（文字どおり） | **オフ** |
| U3 | 検索結果の件数の上限 | 1,000 / 10,000 / 上限なし | **10,000 件** |
| U4 | 変換中の検索 | 許可（注意を表示） / 禁止 | **許可**（注意を表示） |
| U5 | `2_検索.bat`・`検索ワード.txt`（複数ワードの一括検索） | 残す / 廃止 | **廃止**（決定済み。`2_検索.bat`・`検索ワード.txt` は削除した） |
| U6 | 元のファイルを開くときの Excel | 起動中の Excel を使う / 常に新しく起動する / 既定のアプリで開くだけ | **起動中の Excel を使う**（無ければ起動）。該当セルを選択できる |
| U7 | ファイル出力の対象 | 絞り込み後の表示分 / 全件 | **絞り込み後の表示分** |

---

## 15. 既知の制約

確度: ◎ = 動作確認済み、○ = コード上明らか、△ = 推定。

| # | 内容 | 確度 |
|---|---|---|
| 1 | ~~`1_変換.bat` を直接実行した場合、画面からは「変換中」と判定できない。画面から変換を起動すると二重起動になりうる~~ 解消：実行中の変換を `Win32_Process` のコマンドラインで探し、見つかればその進み具合を表示する（3.1） | – |
| 2 | ~~`1_変換.bat` は最後に `pause` するため、コンソールを閉じるまで「変換中」の表示が続く~~ 解消：変換はウィンドウなしで実行し、`pause` しない（`1_変換.bat` は廃止した） | – |
| 3 | 元のファイルのパスは、変換時に記録した変換対象フォルダから組み立てる。変換後に元のフォルダを移動・名前変更した場合は、開くときにフォルダを選び直す必要がある（選んだフォルダは置き換えとして記録し、同じフォルダの下は次から聞かない。4.5.2） | ○ |
| 4 | `元のフォルダ.txt` の無い古いインデックス（変換を 1 回実行すると作られる）を別の場所にコピーした場合は、元の場所が分からないため、開くときにフォルダを選ぶ必要がある | ○ |
| 5 | バックグラウンドの判定は `MainWindowHandle` による。画面に表示中でも、すべてのウィンドウを閉じた直後などはバックグラウンドと判定される（5.1） | △ |
| 6 | フォルダ選択ダイアログは .NET Framework 標準の `FolderBrowserDialog`（ツリー形式）。パスの貼り付けは入力欄側で受ける（`normalizeFolderPath`）。※実行時コンパイル（csc.exe）を無くすため、以前の COM `IFileOpenDialog`（エクスプローラー形式・パス貼り付け可）は廃止した | ○ |
| 7 | TSV が非常に多い場合、件数を数え終わるまで状態表示が `確認中…` になる | △ |

---

> **16. テスト**（16.1 単体テスト・16.2 画面の確認）→ [03_画面_6_実装とテスト_1_テスト.md](03_画面_6_実装とテスト_1_テスト.md#16-テスト)
