# 画面（GUI）設計書 ― 共通仕様

本書は [03_画面.md](03_画面.md) の一部である。章・節の番号は 03_画面.md と分割した各ファイルで通し番号とし、各章がどのファイルにあるかは [03_画面.md](03_画面.md#ファイル構成) の表のとおり。

---

## 8. 設定ファイルの読み書き

| ファイル | 画面での扱い |
|---|---|
| `setting.config` | 画面が読み書きし、変換処理は変換対象フォルダを読む。UTF-8 の JSON で、キーは次のとおり。<br>・`targetFolders`：変換対象フォルダ `{name, path, enabled}` の配列（［1 インデックス管理］。`getTargetFolders` / `writeTargetFolders`）。`name` はインデックス名、`path` は今の置き場所（分けて持つ）。`enabled` が `false` はチェックなし<br>・`indexSources`：変換しないインデックスの元のフォルダ `{name, path}` の配列（[4.5.2](03_画面_2_検索タブ_1_元のファイルを開く.md#452-元のファイルが見つからないとき元のフォルダを設定する)。`readIndexSources` / `writeIndexSources` / `setIndexSourceFolder`）<br>・`searchExcludes`：検索対象のツリーでチェックを外したフォルダ `{path, subfolders}` の配列（4.8。`readSearchExcludes` / `writeSearchExcludes`）。無ければすべてチェックあり<br>・`useRegex`：［正規表現を使う］の状態（true / false。`readSearchOption` / `writeSearchOption`）。無ければオフ<br>・`caseSensitive`：［大文字と小文字を区別］の状態（true / false。同上）。無ければオフ<br>・`fileFilter`：「対象ファイル」の指定（文字列。同上）。無ければ空（すべて）<br>・`openMode`：［開き方］の状態（`normal` / `readOnly` / `new`。`readOpenMode` / `writeOpenMode`、[4.5](03_画面_2_検索タブ_1_元のファイルを開く.md#45-元のファイルを開く)）。無い・知らない値なら `normal`（通常） |
| `config/検索ワード.txt` | **使わない**（`2_検索.bat` とともに廃止した） |

| 項目 | 仕様 |
|---|---|
| 読み込み | `setting.config` が無ければ、以前の設定ファイル（`config/変換対象フォルダパス.txt`・`検索オプション.txt`）を最初に読むときに自動で `setting.config` へ移す。以降、以前のファイルは読まない。どちらも無ければ既定値（空・オフ）とし、**ファイルを作成しない** |
| 保存のタイミング | インデックス一覧：作成・削除・チェックの変更のたび。検索対象インデックス：ダイアログの［OK］。検索対象のツリー：チェックを変えたとき。正規表現の使用・大文字と小文字の区別：チェックを変えたとき。［開き方］：選び直したとき（起動時の読み込みでは保存しない）。対象ファイル：欄からフォーカスが外れたときと検索したとき。インデックスの元のフォルダ：［編集…］で変えたとき・検索結果からフォルダを選んで見つかったとき。インデックス名：インデックスを作成したとき・［編集…］で変えたとき（名前の無い設定を読み込んだときは、読み込み時に割り当てて保存する） |
| 保存形式 | `setting.config`：保存のたびにファイルを読み直し、変えたキーだけを書き換える（ほかのキーは、ほかの画面・処理が保存した内容を保つ）。UTF-8（BOM なし） |
| 外部での編集 | ウィンドウがアクティブになったとき、保存されているインデックス一覧を、画面が最後に読み込み・保存した一覧と比べ（`getTargetsKey`）、異なれば読み直す（画面側の変更はその場で保存済みのため、失われるものは無い）。検索対象インデックスは検索のたびに読む |

---

## 9. キーボード操作

| キー | 動作 |
|---|---|
| Enter（検索ワード欄） | 検索する |
| Esc | 検索中なら中止する（ウィンドウは閉じない） |
| Ctrl+F | 検索ワード欄にフォーカスを移し、全選択する（［2 検索］タブに切り替える） |
| Ctrl+Shift+F | 絞り込み欄にフォーカスを移す |
| Enter（結果の表） | 元のファイルの該当箇所を開く |
| Ctrl+C（結果の表） | 選択行をコピーする（4.5） |
| F5 | ［1 インデックス管理］では状態を、［9 プロセス停止］では一覧を取り直す |
| Ctrl+Tab / Ctrl+Shift+Tab | タブを切り替える |
| Delete | 検索対象インデックスの設定ダイアログで、選択中のフォルダを削除する |
| スペース | 検索対象のツリーで、選択中の項目のチェックを切り替える（4.9） |

変換と強制終了は影響が大きいため、ショートカットキーを設けない。

---

## 10. 表示・アクセシビリティ

- フォントは Yu Gothic UI（無ければ Meiryo UI）、13 px とする。結果の表は 12 px とする。
- WPF は高 DPI に自動で対応するため、表示倍率 125 %・150 % でもぼやけない。
- 状態は**色だけで表さない**。`✓` `✗` `⚠` `⏸` の記号と文言を併記する。一致箇所の強調は背景色に加えて太字にする。
- 文言は、対象が Word・PowerPoint にも広がったため「Excel」ではなく「Office ファイル」とする。
- 入力の誤りや注意は入力欄の下やステータスに表示する。ダイアログは確認が必要な操作（3 章の［削除］、3.4 の変換の確認、3.7 の変換の中止、4.5.2、5.2）に限る。なお、インデックスの［新規作成…］［編集…］は入力が必要なためダイアログにし、入力の誤りはダイアログ内に表示する（3.2）。
- 危険な操作（［すべて終了］）のボタンは赤枠にし、主ボタン（青）と区別する。
- アイコンは `scripts/win_grep.ico`（16〜256 px を収めたマルチサイズ）とし、タイトルバー・タスクバー・Alt+Tab に表示する。
  - XAML には書かず `config_gui.ps1` の `loadWindow` で読み込む（`XamlReader.Load` は XAML 内の相対パスを解決できないため）。ファイルが無くても画面は開く。
  - タスクバー・タイトルバーのアイコンは `Window.Icon`（上記）で出る。画面は `powershell.exe` で動くため、タスクバーのボタンは PowerShell と同じグループにまとめられる（アイコン自体は win_grep のものが出る）。※以前は `SetCurrentProcessExplicitAppUserModelID`（P/Invoke）で独立したボタンにしていたが、実行時コンパイル（csc.exe）を無くすため廃止した（[03_画面_6_実装とテスト.md 12.2](03_画面_6_実装とテスト.md#122-実行時コンパイルcscexe-を使わない)）。
  - `win_grep.bat` 自体のアイコン（エクスプローラー上の表示）は Windows の仕様で変更できない。デスクトップ等にアイコン付きで置きたい利用者は、自分でショートカットを作る（ツールはショートカットを作らない）。

---

## 10.1 配布と実行ポリシー（Mark-of-the-Web）

**配布は zip、実行ポリシーは `RemoteSigned` 前提**とし、`-ExecutionPolicy Bypass` は使わない（未署名＋Bypass＋Hidden は資産管理・EDR がマルウェア類似として注目しやすい組み合わせのため）。署名はしない方針。

- **なぜ Mark-of-the-Web（MOTW）が問題か**：zip をブラウザ・メールでダウンロードして展開すると、展開後の各ファイルに「外部由来」の印（`Zone.Identifier` という代替データストリーム）が付く。`RemoteSigned` は、この印の付いた未署名 `.ps1` の実行をブロックする（`.xaml`・`.ico` は実行ポリシーの対象外なので影響しない）。対象になるのは実行される `.ps1`（`config_gui.ps1`・`common.ps1`・`office_to_tsv.ps1`・`office_reader.ps1`）。
- **対策**：`win_grep.bat` が起動前に、自フォルダ配下の印を `Get-ChildItem -Recurse -File \| Unblock-File` で消す。インラインの `-Command` は実行ポリシーの対象外なので、印が付いていてもこの行は動く。念のため `config_gui.ps1` も起動時に自フォルダの印を消す（利用者が自作したショートカットから起動したときや、あとでファイルを差し替えたときのため）。
- **初回のセキュリティ警告**：zip 由来だと `win_grep.bat` 自体にも印が付くため、初回のダブルクリックで Windows の「開いているファイル - セキュリティの警告」（発行元を確認できません）が 1 回出る。［実行］を押すと bat が動き、以降は印が消えるので警告は出ない。これは Windows の正規の確認であり、隠す対象ではない。
- **`Bypass` を使わない代わりの実測挙動**（この PC・`RemoteSigned` で確認）：印あり＝ブロック（「デジタル署名されていません。実行できません」）／`win_grep.bat` の `Unblock-File` で印を消す＝0 件／消した後は `RemoteSigned` で起動成功。
- **バッチの書式**：`win_grep.bat` は ASCII・CRLF で書く。cmd はコードページ（日本語環境は CP932）と改行に敏感で、UTF-8 の日本語コメントや LF 改行だと行が結合して `Unblock-File` 行がコメントに飲み込まれる不具合が出るため。説明コメントは英語にし、日本語の説明は本書に置く。

---

## 11. メッセージ一覧

| 表示場所 | 条件 | 文言 |
|---|---|---|
| 一覧の中 | インデックスが 1 件も無い | `インデックスがありません。［新規作成…］で、Office ファイルのあるフォルダを指定してください。` |
| 一覧の「フォルダの状態」 | フォルダがある / 無い | `✓ フォルダがあります` / `✗ フォルダが見つかりません` |
| 新規作成・編集のダイアログ | 入力に誤りがある | 3.2 のとおり（ダイアログ内に赤字） |
| ステータス | インデックスを作成した | `インデックス [<名前>] を作成しました。［変換を開始］を押すと中身を変換します` |
| ステータス | 作成したがフォルダが無い | `インデックス [<名前>] を作成しましたが、フォルダが見つかりません：<パス>` |
| ステータス | 同じフォルダのインデックスがある | `「<パス>」のインデックス [<名前>] は既にあります` |
| ステータス | インデックスを編集した | `インデックスを変更しました（名前 [<旧>] → [<新>] / 場所 <旧> → <新>）` |
| ステータス | インデックスの TSV を削除している（別スレッド） | `インデックス [<名前>] の TSV を削除しています…（件数によっては少し時間がかかります）` |
| ステータス | インデックスを削除した | `インデックス [<名前>] を削除しました` |
| ステータス | 削除に失敗した | `インデックス [<名前>] の削除に失敗しました：<メッセージ>` |
| ステータス | 変換対象を数えている | `変換対象を確認しています…` |
| ステータス | 確認で［変換を開始］を押した / 取りやめた | `変換を開始しました` / `変換を取りやめました` |
| 確認 | インデックスの削除 | 3 章 No.4 のとおり |
| 確認 | 変換の内容 | 3.4 のとおり（インデックスごとの変換対象の件数） |
| 警告 | 変換中に作成・編集・削除しようとした | `変換中はインデックスを<作成／編集／削除>できません。変換が終わるまでお待ちください（［中止］で止められます）。` |
| 警告 | 前のインデックスの削除中に操作しようとした | `前のインデックスの削除が終わるまでお待ちください。` |
| インデックスの状態 | 6.1 のとおり | 6.1 のとおり |
| 変換の進み具合 | 確認待ち / 変換中 / 終わった | 3.6・3.7 のとおり |
| 検索ワード欄の下 | 正規表現オンで不正なワード | `正規表現として不正なため、文字どおり検索します。` |
| 件数（No.5） | 検索中 / 完了 / 絞り込み中 | `検索中… N / M ファイル` / `N 件（M ファイル） ・ X 秒` / `N 件中 K 件を表示` |
| 件数（No.5） | 0 件 | `見つかりませんでした。`（正規表現オフで記号を含むワードなら、`正規表現として探す場合は［正規表現を使う］をオンにしてください。` を添える。そうでなく、既定から変えた検索条件があれば `（検索条件：大文字と小文字を区別・対象ファイル：<指定>）` を添える） |
| 件数（No.5） | 対象ファイルに一致するファイルが無い | `対象ファイル（<指定>）に一致するファイルがありません。` |
| ステータス | 検索が終わった | `検索しました（<ワード>：N 件）`。既定から変えた検索条件があれば `検索しました（<ワード>：N 件　条件：<条件>）` |
| ステータス | 正規表現の照合が終わらない（1 行 5 秒超） | `検索できませんでした：正規表現の照合に時間がかかりすぎるため、検索を中止しました。正規表現を見直してください。` |
| ステータス | 中止した / 上限に達した | `中止しました（N 件まで表示）` / `10,000 件を超えたため、ここで打ち切りました。この先のファイルは検索していないため、ワード・対象ファイル・検索対象で絞り込んでください。` |
| ステータス | 変換中に検索した | `変換中のため、作成途中のインデックスを検索しています。` |
| 確認 | 元のファイルが無い・元の場所が分からない | フォルダを選ぶかの確認（[4.5.2](03_画面_2_検索タブ_1_元のファイルを開く.md#452-元のファイルが見つからないときフォルダの置き換え) のメッセージ） |
| ステータス | 元のファイルが無い（フォルダを選ばなかった） | `元のファイルが見つかりません：<パス>` |
| ステータス | 選んだフォルダで見つかった | `フォルダを記録しました（<元のフォルダ> → <選んだフォルダ>）` |
| ステータス | 結果を出力した / 書き込めない | `検索結果.txt に出力しました` / `検索結果.txt に書き込めません。開いているアプリを閉じてから、もう一度出力してください。` |
| ステータス | プロセスを終了した / できなかった | `N 個のプロセスを終了しました。` / `PID <N> を終了できませんでした：<理由>` |
| ステータス | 保存した | `インデックス一覧を保存しました（H:mm）` |
| ステータス | インデックス一覧がほかで変更された（8 章） | `インデックス一覧がほかで変更されたため、読み直しました` |
| ダイアログ | 3 章の［新規作成…］［編集…］［削除］、3.4（変換の確認）、3.7（変換の中止・変換中に閉じる・変換できなかった）、4.7（検索対象インデックスの［削除］）、5.2 | 各節のとおり |
| ダイアログ | 保存に失敗した | `保存に失敗しました。<例外メッセージ>` |
| ダイアログ | 多重起動で、すでに開いている画面に知らせられない（以前の版が開いている等） | `すでに開いています。`（知らせられたときは、そのウィンドウを前面に出すだけで、何も表示しない） |
