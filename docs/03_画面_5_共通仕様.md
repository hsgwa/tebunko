# 画面（GUI）設計書 ― 共通仕様

本書は [03_画面.md](03_画面.md) の一部である。章・節の番号は 03_画面.md と分割した各ファイルで通し番号とし、各章がどのファイルにあるかは [03_画面.md](03_画面.md#ファイル構成) の表のとおり。

---

## 8. 設定ファイルの読み書き

| ファイル | 画面での扱い |
|---|---|
| `setting.config` | 画面が読み書きし、インデクサはクロール対象フォルダを読む。UTF-8 の JSON で、検索のキーは次のとおり（比較のキーは `diff` で始まる。[05_比較.md](05_比較.md)）。<br>・`targetFolders`：クロール対象フォルダ `{name, path, enabled}` の配列（［インデックス管理］。`getTargetFolders` / `writeTargetFolders`）。`name` はインデックス名、`path` は今の置き場所（分けて持つ）。`enabled` が `false` はチェックなし<br>・`indexSources`：取り込まないインデックスの元のフォルダ `{name, path}` の配列（[4.5.2](03_画面_2_検索タブ_1_元のファイルを開く.md#452-元のファイルが見つからないとき元のフォルダを設定する)。`readIndexSources` / `writeIndexSources` / `setIndexSourceFolder`）<br>・`searchExcludes`：検索対象のツリーでチェックを外したフォルダ `{path, subfolders}` の配列（4.8。`readSearchExcludes` / `writeSearchExcludes`）。無ければすべてチェックあり<br>・`useRegex`：［正規表現を使う］の状態（true / false。`readSearchOption` / `writeSearchOption`）。無ければオフ<br>・`caseSensitive`：［大文字と小文字を区別］の状態（true / false。同上）。無ければオフ<br>・`fileFilter`：「対象ファイル」の指定（文字列。同上）。無ければ空（すべて）<br>・`includeShapes` / `includeComments`：［図形も検索］［コメントも検索］の状態（true / false。同上）。無ければオン<br>・`openMode`：［開き方］の状態（`normal` / `readOnly` / `new`。`readOpenMode` / `writeOpenMode`、[4.5](03_画面_2_検索タブ_1_元のファイルを開く.md#45-元のファイルを開く)）。無い・知らない値なら `normal`（通常） |
| `config/検索ワード.txt` | **使わない**（`2_検索.bat` とともに廃止した） |

| 項目 | 仕様 |
|---|---|
| 読み込み | `setting.config` が無ければ、以前の設定ファイル（`config/変換対象フォルダパス.txt`・`検索オプション.txt`）を最初に読むときに自動で `setting.config` へ移す。以降、以前のファイルは読まない。どちらも無ければ既定値（空・オフ）とし、**ファイルを作成しない** |
| 保存のタイミング | インデックス一覧：作成・削除・チェックの変更のたび。検索対象インデックス：ダイアログの［OK］。検索対象のツリー：チェックを変えたとき。正規表現の使用・大文字と小文字の区別：チェックを変えたとき。［開き方］：選び直したとき（起動時の読み込みでは保存しない）。対象ファイル：欄からフォーカスが外れたときと検索したとき。インデックスの元のフォルダ：［編集…］で変えたとき・検索結果からフォルダを選んで見つかったとき。インデックス名：インデックスを作成したとき・［編集…］で変えたとき（名前の無い設定を読み込んだときは、読み込み時に割り当てて保存する） |
| 保存形式 | `setting.config`：保存のたびにファイルを読み直し、変えたキーだけを書き換える（ほかのキーは、ほかの画面・処理が保存した内容を保つ。検索が知らない比較の `diff*` のキーも消さない。`writeSettingsFile`）。UTF-8（BOM なし） |
| 外部での編集 | ウィンドウがアクティブになったとき、保存されているインデックス一覧を、画面が最後に読み込み・保存した一覧と比べ（`getTargetsKey`）、異なれば読み直す（画面側の変更はその場で保存済みのため、失われるものは無い）。検索対象インデックスは検索のたびに読む |

---

## 9. キーボード操作

| キー | 動作 |
|---|---|
| Enter（検索ワード欄） | 検索する |
| Esc | 検索中なら中止する（ウィンドウは閉じない）。［2 比較］では比較中なら中止する |
| Ctrl+F | どのタブからでも、［1 検索］→［検索］タブに切り替え、検索ワード欄にフォーカスを移して全選択する |
| Ctrl+Shift+F | どのタブからでも、［1 検索］→［検索］タブに切り替え、絞り込み欄にフォーカスを移す |
| Enter（結果の表） | 元のファイルの該当箇所を開く（ファイルの見出しの行では、そのファイルの行を開く・閉じる。4.9） |
| Ctrl+C（結果の表） | 選択行をコピーする（4.5） |
| F5 | タブごとに取り直す。［1 検索］ではインデックス作成の状態・インデックスの件数・検索対象のツリーを、［9 プロセス停止］では一覧を取り直す。［2 比較］では同じ条件で比べ直す |
| Ctrl+1 / Ctrl+2 / Ctrl+9 | 1 段目のタブ（［1 検索］［2 比較］［9 プロセス停止］）を切り替える |
| Ctrl+Tab / Ctrl+Shift+Tab | WPF の標準の動き。フォーカスのあるタブの段（［1 検索］の中なら 2 段目の［インデックス管理］［検索］）を切り替える |
| Delete | 検索対象インデックスの設定ダイアログで、選択中のフォルダを削除する |
| スペース | 検索対象のツリーで、選択中の項目のチェックを切り替える（4.9） |

インデックス作成と強制終了は影響が大きいため、ショートカットキーを設けない。［2 比較］だけのキー（F8 / Shift+F8 で次・前の変更へなど）は [05_比較.md](05_比較.md) を参照。キーはウィンドウの `PreviewKeyDown`（`tebunko/gui.ps1`）で受け、［1 検索］の分は `onGrepKeyDown`（`tebunko_grep/ui/grep_page.ps1`）、［2 比較］の分は `onDiffKeyDown` に渡す。

フォルダ選択ダイアログ（[3.2.1](03_画面_1_インデックス管理タブ.md#321-フォルダ選択ダイアログ参照)）の中だけは、**エクスプローラーと同じキー・マウス操作**にする（`Alt+←` / `Alt+→` / マウスのサイドボタン＝戻る・進む、`Alt+↑` / `BackSpace`＝1 つ上へ、`F5` / `Ctrl+R`＝読み直す、`F4` / `Alt+D` / `Ctrl+L`＝アドレスバーへ）。

---

## 10. 表示・アクセシビリティ

- フォントは Yu Gothic UI（無ければ Meiryo UI）、13 px とする。結果の表は 12 px とする。
- WPF は高 DPI に自動で対応するため、表示倍率 125 %・150 % でもぼやけない。
- 状態は**色だけで表さない**。`✓` `✗` `⚠` `⏸` の記号と文言を併記する。一致箇所の強調は背景色に加えて太字にする。
- 文言は、対象が Word・PowerPoint にも広がったため「Excel」ではなく「Office ファイル」とする。
- 入力の誤りや注意は入力欄の下やステータスに表示する。ダイアログは確認が必要な操作（3 章の［削除］、3.4 のインデックス作成の確認、3.7 のインデックス作成の中止、4.5.2、5.2）に限り、その形式は 10.2 のとおりとする（3.4 だけは、インデックスごとの件数を一覧で見せるため専用のダイアログにする）。なお、インデックスの［追加…］［編集…］は入力が必要なためダイアログにし、入力の誤りはダイアログ内に表示する（3.2）。
- 危険な操作（［すべて終了］）のボタンは赤枠にし、主ボタン（青）と区別する。確認ダイアログの中で消す操作を選ぶボタン（［削除する］など）は、赤で塗りつぶして区別する（10.2）。
- **説明を読まないと何が起きるか分からない画面にしない**。確認は「実行するとこうなります」を並べて見せ（10.2）、ボタンの文言は「はい／いいえ」ではなく操作そのもの（［削除する］［中止する］）にする。画面の注意書きには、内部の言葉（`TSV`・`work\index`・`取り込み一覧`）や、その場で必要のない手順の説明を書かない。
- アイコンは `scripts/tebunko/tebunko.ico`（16〜256 px を収めたマルチサイズ）とし、タイトルバー・タスクバー・Alt+Tab に表示する。
  - XAML には書かず `tebunko/gui.ps1` の `${iconFile}` を `loadWindow` で読み込む（`XamlReader.Load` は XAML 内の相対パスを解決できないため）。ファイルが無くても画面は開く。
  - タスクバー・タイトルバーのアイコンは `Window.Icon`（上記）で出る。画面は `powershell.exe` で動くため、タスクバーのボタンは PowerShell と同じグループにまとめられる（アイコン自体は tebunko のものが出る）。※以前は `SetCurrentProcessExplicitAppUserModelID`（P/Invoke）で独立したボタンにしていたが、実行時コンパイル（csc.exe）を無くすため廃止した（[03_画面_6_実装とテスト.md 12.2](03_画面_6_実装とテスト.md#122-実行時コンパイルcscexeを使わない)）。
  - `tebunko.bat`・`tebunko_grep.bat` 自体のアイコン（エクスプローラー上の表示）は Windows の仕様で変更できない。デスクトップ等にアイコン付きで置きたい利用者は、自分でショートカットを作る（ツールはショートカットを作らない）。

---

## 10.1 配布と実行ポリシー（Mark-of-the-Web）

**配布は zip、実行ポリシーは `RemoteSigned` 前提**とし、`-ExecutionPolicy Bypass` は使わない（未署名＋Bypass＋Hidden は資産管理・EDR がマルウェア類似として注目しやすい組み合わせのため）。署名はしない方針。

- **なぜ Mark-of-the-Web（MOTW）が問題か**：zip をブラウザ・メールでダウンロードして展開すると、展開後の各ファイルに「外部由来」の印（`Zone.Identifier` という代替データストリーム）が付く。`RemoteSigned` は、この印の付いた未署名 `.ps1` の実行をブロックする（`.xaml`・`.ico` は実行ポリシーの対象外なので影響しない）。対象になるのは実行される `.ps1`（`tebunko/gui.ps1`・`tebunko_grep/lib.ps1`・`tebunko_grep/indexer.ps1`・`tebunko_diff/lib.ps1`・`tebunko_diff/differ.ps1`・`shared/` の各ファイルなど、`scripts` 配下のすべて）。
- **対策**：起動用バッチ（`tebunko.bat`。互換用の `tebunko_grep.bat` も同じ）が起動前に、自フォルダの `scripts` 配下の印を `Get-ChildItem -Recurse -File \| Unblock-File` で消す（`work` には数万件の TSV があり、起動のたびに全件へかけると TSV 1 万件あたり約 3 秒遅くなるため、実行するスクリプトのある `scripts` に限る）。インラインの `-Command` は実行ポリシーの対象外なので、印が付いていてもこの行は動く。念のため `tebunko/gui.ps1` も起動時に `scripts` 配下の印を消す（利用者が自作したショートカットから起動したときや、あとでファイルを差し替えたときのため）。
- **初回のセキュリティ警告**：zip 由来だと `tebunko.bat` 自体にも印が付くため、初回のダブルクリックで Windows の「開いているファイル - セキュリティの警告」（発行元を確認できません）が 1 回出る。［実行］を押すと bat が動き、以降は印が消えるので警告は出ない。これは Windows の正規の確認であり、隠す対象ではない。
- **`Bypass` を使わない代わりの実測挙動**（この PC・`RemoteSigned` で確認）：印あり＝ブロック（「デジタル署名されていません。実行できません」）／起動用バッチの `Unblock-File` で印を消す＝0 件／消した後は `RemoteSigned` で起動成功。
- **コンソールの窓を残さない**：起動用バッチ（`tebunko.bat`・`tebunko_grep.bat`）は画面（`tebunko/gui.ps1`）を `start "" conhost.exe powershell ... -WindowStyle Hidden` で起動する。Windows 11 で既定のターミナルが Windows Terminal のとき（「Windows に任せる」の場合も含む）、コンソールの窓は Windows Terminal に渡され、Windows Terminal は `-WindowStyle Hidden` を無視する。そのため `conhost.exe` を通さないと、画面を閉じるまで PowerShell の窓（Windows Terminal のタブ）が開いたままになる。`conhost.exe` を通すと従来のコンソールで動き、`-WindowStyle Hidden` で窓が隠れる（起動の瞬間に一度だけ窓が見えることがある）。インデクサは画面から `Start-Process -WindowStyle Hidden` で最初から隠して起動するため、Windows Terminal に渡されず、`conhost.exe` は要らない。
- **バッチの書式**：`tebunko.bat`・`tebunko_grep.bat` は ASCII・CRLF で書く。cmd はコードページ（日本語環境は CP932）と改行に敏感で、UTF-8 の日本語コメントや LF 改行だと行が結合して `Unblock-File` 行がコメントに飲み込まれる不具合が出るため。説明コメントは英語にし、日本語の説明は本書に置く。

---

## 10.2 確認ダイアログ（`showConfirm`）

取り消せない操作・時間のかかる操作の確認は、`MessageBox` ではなく `scripts/shared/xaml/dialog_confirm.xaml` の自前のダイアログで行う（`showConfirm`）。**説明を読まなくても、押す前に何が起きるか分かる**ようにするため。

| 部分 | 役割 | 書き方 |
|---|---|---|
| 見出し | これから何をするか | 1 行で読み切れる長さの問いかけ（`インデックス「<名前>」を一覧から削除しますか？`） |
| 結果 | 実行すると何がどうなるか | `✗`（消える・失われる、赤）・`✓`（残る、緑）・`→`（続けて起きること、青）の行。各行は「何がどうなるか」の 1 行と、場所・件数などの補足の 1 行（補足は空なら出さない） |
| 選択肢 | 押せる操作 | ボタンの文言そのものが操作（［削除する］［作り直す］［中止する］）。2 つ以上あるときは縦に並べ、ボタンの中の 2 行目にその結果を書く |
| 補足 | 別のやり方の案内 | 読まなくても操作できる内容だけを書く（`しばらく検索しないだけなら、削除せずに［作成］のチェックを外してください。…`） |

- **ボタンの並べ方**: 選択肢が 1 つなら右下に［実行］＋［キャンセル］、2 つ以上なら本文に選択肢のボタンを縦に並べ、右下は［キャンセル］だけにする（キャンセルの文言は場面に合わせて変える。例：［閉じない］［インデックス作成を始めない］）。
- **既定のボタン**: 消す操作（`Danger`。ボタンを赤にする）・時間のかかる操作（`Careful`）・選択肢が 2 つ以上のときは、Enter で進まないよう［キャンセル］を既定にしてフォーカスを置く。そうでなければ主ボタンを既定にする。
- **文言の決まり**: 画面に出ない言葉（`TSV`・`work\index`・`取り込み一覧`）は書かない。利用者から見て何が消えて何が残るかだけを書く。「〜の場合は〜してください」という手順の説明は、結果ではなく補足に置く。
- **使っている場面**: インデックスの［削除］（[3 章](03_画面_1_インデックス管理タブ.md)）、インデックス作成の中止・インデックス作成中に閉じる（3.7）、元のファイルが見つからないとき（[4.5.2](03_画面_2_検索タブ_1_元のファイルを開く.md#452-元のファイルが見つからないとき元のフォルダを設定する)）、プロセスの終了（[5.2](03_画面_3_プロセス停止タブ.md#52-終了の確認)）。インデックス作成の確認（3.4）は、インデックスごとの件数を一覧で見せるため専用のダイアログにする。
- 伝えるだけのダイアログ（エラー・警告・`OK` だけのもの）は、これまでどおり `showMessage`（`MessageBox`）で出す。

---

## 11. メッセージ一覧

| 表示場所 | 条件 | 文言 |
|---|---|---|
| 一覧の中 | インデックスが 1 件も無い | `インデックスがありません。［追加…］で、Office ファイルのあるフォルダを指定してください。` |
| 一覧の「フォルダの状態」 | フォルダがある / 無い / 調べている | `✓ フォルダがあります` / `✗ フォルダが見つかりません` / `… フォルダを確認しています` |
| 追加・編集のダイアログ | 入力に誤りがある | 3.2 のとおり（ダイアログ内に赤字） |
| ステータス | インデックスを追加した | `インデックス [<名前>] を追加しました。［インデックス作成を開始］を押すと中身を取り込みます` |
| ステータス | 追加したがフォルダが無い | `インデックス [<名前>] を追加しましたが、フォルダが見つかりません：<パス>` |
| ステータス | 同じフォルダのインデックスがある | `「<パス>」のインデックス [<名前>] は既にあります` |
| ステータス | インデックスを編集した | `インデックスを変更しました（名前 [<旧>] → [<新>] / 場所 <旧> → <新>）` |
| ステータス | インデックスの TSV を削除している（別スレッド） | `インデックス [<名前>] の TSV を削除しています…（件数によっては少し時間がかかります）` |
| ステータス | インデックスを削除した | `インデックス [<名前>] を削除しました` |
| ステータス | 削除に失敗した | `インデックス [<名前>] の削除に失敗しました：<メッセージ>` |
| ステータス | クロールしている | `クロールしています…` |
| ステータス | 確認で［インデックス作成を開始］を押した / 取りやめた | `インデックス作成を開始しました` / `インデックス作成を取りやめました` |
| 確認 | インデックスの削除 | 3 章 No.4 のとおり |
| 確認 | インデックス作成の内容 | 3.4 のとおり（インデックスごとの取り込み対象の件数） |
| 警告 | インデックス作成中に追加・編集・削除しようとした | `インデックス作成中はインデックスを<追加／編集／削除>できません。インデックス作成が終わるまでお待ちください（［中止］で止められます）。` |
| 警告 | 前のインデックスの削除中に操作しようとした | `前のインデックスの削除が終わるまでお待ちください。` |
| インデックスの状態 | 6.1 のとおり | 6.1 のとおり |
| インデックス作成の進み具合 | 確認待ち / インデックス作成中 / 終わった | 3.6・3.7 のとおり |
| 検索ワード欄の下 | 正規表現オンで不正なワード | `正規表現として不正なため、文字どおり検索します。` |
| 件数（No.5） | 検索中 / 完了 / 絞り込み中 | `検索中… N / M ファイル` / `N 件（M ファイル） ・ X 秒` / `N 件中 K 件を表示` |
| 件数（No.5） | 0 件 | `見つかりませんでした。`（正規表現オフで記号を含むワードなら、`正規表現として探す場合は［正規表現を使う］をオンにしてください。` を添える。そうでなく、既定から変えた検索条件があれば `（検索条件：大文字と小文字を区別・対象ファイル：<指定>）` を添える） |
| 件数（No.5） | 対象ファイルに一致するファイルが無い | `対象ファイル（<指定>）に一致するファイルがありません。` |
| ステータス | 検索が終わった | `検索しました（<ワード>：N 件）`。既定から変えた検索条件があれば `検索しました（<ワード>：N 件　条件：<条件>）` |
| ステータス | 正規表現の照合が終わらない（1 行 5 秒超） | `検索できませんでした：正規表現の照合に時間がかかりすぎるため、検索を中止しました。正規表現を見直してください。` |
| ステータス | 中止した / 上限に達した | `中止しました（N 件まで表示）` / `10,000 件を超えたため、ここで打ち切りました。この先のファイルは検索していないため、ワード・対象ファイル・検索対象で絞り込んでください。` |
| ステータス | インデックス作成中に検索した | `インデックス作成中のため、作成途中のインデックスを検索しています。` |
| 確認 | 元のファイルが無い・元の場所が分からない | フォルダを選ぶかの確認（[4.5.2](03_画面_2_検索タブ_1_元のファイルを開く.md#452-元のファイルが見つからないとき元のフォルダを設定する) のメッセージ） |
| ステータス | 元のファイルが無い（フォルダを選ばなかった） | `元のファイルが見つかりません：<パス>` |
| ステータス | 選んだフォルダで見つかった | `フォルダを記録しました（<元のフォルダ> → <選んだフォルダ>）` |
| ステータス | 結果を出力した / 書き込めない | `検索結果.txt に出力しました` / `検索結果.txt に書き込めません。開いているアプリを閉じてから、もう一度出力してください。` |
| ステータス | プロセスを終了した / できなかった | `N 個のプロセスを終了しました。` / `PID <N> を終了できませんでした：<理由>` |
| ステータス | 保存した | `インデックス一覧を保存しました（H:mm）` |
| ステータス | インデックス一覧がほかで変更された（8 章） | `インデックス一覧がほかで変更されたため、読み直しました` |
| ダイアログ | 3 章の［追加…］［編集…］［削除］、3.4（インデックス作成の確認）、3.7（インデックス作成の中止・インデックス作成中に閉じる・インデックスを作成できなかった）、4.5.2（元のファイルが見つからないとき）、5.2。確認のものは 10.2 の形式で出す（3.4 は一覧を見せるため専用のダイアログ） | 各節のとおり |
| ダイアログ | 保存に失敗した | `保存に失敗しました。<例外メッセージ>` |
| ダイアログ | 多重起動で、すでに開いている画面に知らせられない（以前の版が開いている等） | `すでに開いています。`（知らせられたときは、そのウィンドウを前面に出すだけで、何も表示しない） |
