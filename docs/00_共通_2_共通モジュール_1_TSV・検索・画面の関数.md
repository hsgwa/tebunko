# win_grep 設計書（共通モジュール ― TSV・検索・画面の関数）

本書は [00_index.md](00_index.md) の一部である。章・節の番号は 00_index.md と分割した各ファイルで通し番号とし、各章がどのファイルにあるかは [00_index.md](00_index.md#ドキュメント構成) の表のとおり。

---

## 5. 共通モジュール `scripts/common.ps1`（続き）

### 5.2 関数一覧（続き）

5.2.1 設定ファイル・変換一覧・変換対象フォルダ・インデックス名は [00_共通_2_共通モジュール.md](00_共通_2_共通モジュール.md#521-設定ファイル変換一覧変換対象フォルダインデックス名) を参照。

#### 5.2.2 TSV の作成・検索

| 関数 | 入力 | 出力 | 概要 | 詳細 | 使用元 |
|---|---|---|---|---|---|
| `toSafeFileName` | name | string | ファイル名禁止文字を全角に置換 | [01_変換.md](01_変換.md) | インデックス名（newIndexName）、以前の版のインデックスのシート名の照合（画面） |
| `encodeIndexPlace` | place | string | TSV のファイル名に入れる場所を符号化する（ファイル名禁止文字・制御文字・`_`・`%` を `%XX` に） | [01_変換.md 6.2 節](01_変換_7_出力TSVと既知の問題.md#62-場所の符号化encodeindexplace--decodeindexplace) | toIndexFileName |
| `decodeIndexPlace` | place | string | `encodeIndexPlace` の `%XX` を元に戻す（それ以外の `%` はそのまま） | 同上 | splitIndexFileName（検索処理の C# にも同じ処理 `TsvSearcher.DecodePlace`） |
| `toIndexFileName` | place | string | インデックスの TSV のファイル名 `<場所>.tsv`（場所は `encodeIndexPlace`）。元のファイル名はフォルダ名にするため入れない。`$maxFileNameLength`（255）文字を超えれば例外 | [01_変換.md 6.1 節](01_変換_7_出力TSVと既知の問題.md#61-配置命名規則) | 変換（Excel・Word・PowerPoint） |
| `toLongPath` | path | string | ファイル操作に渡すパスの先頭に `\\?\`（ネットワークのパスは `\\?\UNC\`）を付け、260 文字を超えるパスも扱えるようにする。付いていればそのまま | [01_変換.md 6.1 節](01_変換_7_出力TSVと既知の問題.md#長いパス260-文字超の扱い) | 変換・検索 |
| `fromLongPath` | path | string | `toLongPath` で付けた `\\?\` を外す（`Get-ChildItem` の `FullName` から相対パスを求めるため） | 同上 | 変換・検索 |
| `replaceCellNewLine` | inputString | string | `"` で囲まれた範囲の改行（CRLF・CR・LF）を `$cellNewLine` に置き換える | [01_変換_1_Excel.md](01_変換_1_Excel.md) | formatTsv |
| `formatTsv` | content, firstRow（既定 1）, firstColumn（既定 1） | string | TSV 整形。N 行目・k 列目をシートの N 行目・k 列目にそろえる | [01_変換_1_Excel.md](01_変換_1_Excel.md) | prettyTsv |
| `prettyTsv` | 入力パス, 出力パス, firstRow（既定 1）, firstColumn（既定 1） | bool | 入力（UTF-16）を読み、`formatTsv` して UTF-8（BOM 付き）で保存。内容が空なら保存せず `$false` | [01_変換_1_Excel.md](01_変換_1_Excel.md) | 変換 |
| `splitIndexTsvPath` | relPath | `@{Book; Place; RelDir}` | インデックスフォルダからの TSV の相対パスを、元のファイル名・場所・元のファイルのあるフォルダに分ける。今の形式（`<ファイル名.xlsx>\<場所>.tsv`）と以前の形式（フラット）の両方を扱う | [02_検索.md 5.3 節](02_検索.md#53-ファイル名場所の分解splitindextsvpath) | 画面・検索処理の C# も同じ規則 |
| `splitIndexFileName` | fileName | `@{book; sheet}` | 以前の形式（フラット）のインデックスファイル名を元のファイル名・場所に分解し、場所を `decodeIndexPlace` で元に戻す（正規表現は `$indexFileNamePattern`） | 同上 | splitIndexTsvPath、変換（migrateFlatIndex） |
| `toResultLine` | book, location, lineNumber, line | string | `ファイル名<TAB>場所<TAB>行番号<TAB>該当行` を返す。Excel はセル内改行を LF に戻し、Word・PowerPoint は `"` で始まるセルを `"` で囲む。場所のタブ・改行（Excel のシート名に付けられる）はスペースにする | [02_検索.md](02_検索.md) | 検索 |
| `countTsvFields` | line | int | Excel に貼り付けたときのセル数（`"` で始まるセルは閉じる `"` までを 1 セルとする。先頭のセルが空でも数え落とさない） | [02_検索.md](02_検索.md) | 検索 |
| `toColumnName` | number | string | 列番号を列名に変換（1 → `A`、27 → `AA`） | [02_検索.md](02_検索.md) | toResultHeader |
| `toResultHeader` | columnCount | string | 見出し行 `ファイル名<TAB>場所<TAB>行<TAB>A<TAB>B…` を返す | [02_検索.md](02_検索.md) | 検索 |
| `isValidRegex` | pattern | bool | 正規表現として正しいか | – | 検索・画面 |
| `getIndexTsvFiles` | folders（既定 `getIndexFolders`。フォルダの文字列、または `@{Root; RelPath; Recurse}`（画面のツリーで選んだ範囲。`Recurse` が `$false` なら直下だけ）の配列） | `@{Folders; Files}` | 検索対象の TSV を列挙する。結果の相対パスは `Root` から求める。Folders はフォルダごとの `@{Path; Root; Exists; Count}`、Files は TSV のフルパス → `@{Root; RelPath}`（パス順・重複なし） | [02_検索.md](02_検索.md) | 検索・画面 |
| `testIndexExists` | folders | bool | TSV が 1 件でもあるか（最初の 1 件で打ち切る） | – | 画面 |
| `getIndexSummary` | folders | `@{Count; LastWrite; Missing}` | TSV の件数・最新の更新日時・存在しないフォルダ | – | 画面 |
| `newSearchRegex` | word, simpleMatch, caseSensitive | `@{Regex; SimpleMatch}` | 検索条件から照合用の正規表現を作る（文字どおりならエスケープ、大文字と小文字を区別しないなら IgnoreCase、正規表現として不正なら文字どおりにする）。simpleMatch が偽でも、正規表現として不正なら文字どおりにする。1 行の照合は 5 秒で時間切れ | [02_検索.md 4.2](02_検索.md#42-検索条件サクラエディタの-grep-にならう) | 検索・画面（一致箇所の強調） |
| `newFileFilter` | filter | `@{Include; Exclude}` | 対象ファイルの指定（`*.xlsx;見積;!*old*`）を、元のファイル名に対する正規表現にする（無い側は `$null`） | 同上 | `searchIndex` |
| `initTsvSearcher` | – | – | 検索処理の C#（`WinGrep.TsvSearcher` / `TsvSearchFile` / `TsvSearchHit`）をコンパイルする（同じプロセスでは 1 回だけ） | [02_検索.md 4.3](02_検索.md#43-検索の実装速度) | `searchIndex` |
| `searchIndex` | word, tsvFiles, simpleMatch, limit, chunkSize, onProgress, shouldStop, caseSensitive, fileFilter | `@{Hits; SimpleMatch; Total; Truncated; Cancelled}` | TSV を検索する（照合は C# の `TsvSearcher`）。Hits は `WinGrep.TsvSearchHit`（Root・RelPath・RelDir・FileName・Book・Location・LineNumber・Line）。Total は対象ファイルで絞った後の TSV の数。chunkSize 件ずつ検索して進捗を知らせ、上限・中止に対応する。読めない TSV は飛ばす | [02_検索.md](02_検索.md) | 検索・画面 |
| `toSearchResultLines` | hits | `@{Header; Lines}` | 検索結果ファイルの見出し行と各行（相対フォルダ付き `toResultLine`、最大セル数の `toResultHeader`） | [02_検索.md](02_検索.md) | 検索・画面 |
| `writeSearchResult` | writer, word, hits | – | 1 ワード分の `【検索文字列　X】 N 件`・見出し行・各行・空行を書き出す | [02_検索.md](02_検索.md) | 検索・画面 |
| `splitTsvCells` | line | string[] | TSV の 1 行をセルに分ける（`"` で囲まれたセルは 1 セルとし、囲みを外す） | – | – |
| `readTsvContext` | path, lineNumber, before（既定 3）, after（既定 3） | `@{LineNumber; Line}` の配列 | TSV の lineNumber 行目と前後の行を返す（行の数え方は `searchIndex` と同じ）。必要な行まで読んだら止める。読めなければ空 | [03_画面_2_検索タブ.md](03_画面_2_検索タブ.md) 4.9 | 画面 |

#### 5.2.3 元のファイルの特定・画面

| 関数 | 入力 | 出力 | 概要 | 詳細 | 使用元 |
|---|---|---|---|---|---|
| `writeSourceFolderFile` | folders（`@{Path; Name}` の配列）, dir（既定 `$indexDir`） | – | インデックスのフォルダに `元のフォルダ.txt`（`インデックス名<TAB>変換対象フォルダ`）を書き出す | [01_変換.md](01_変換_4_メインフローと変換一覧.md#42-変換一覧と変換対象の決定差分中断再試行) | 変換 |
| `readSourceFolderFile` | dir | Dictionary（インデックス名 → フォルダパス） | `元のフォルダ.txt` を読む。無ければ空 | 同上 | getSourceFolderMap |
| `getSourceFolderMap` | dir, statusPath（既定 `$statusFile`）, settingsPath（既定 `$settingsFile`） | Dictionary（インデックス名 → 元のフォルダ） | インデックス名に対する元のフォルダ。`元のフォルダ.txt` → （既定のインデックスなら）変換一覧 → **設定（`targetFolders` / `indexSources`）** の順に上書きするため、設定の「今の置き場所」が最も優先される | [03_画面_2_検索タブ.md](03_画面_2_検索タブ_1_元のファイルを開く.md#451-元のフォルダの特定インデックスを別の-pc場所で使う場合) | getSourceLocation |
| `getSourceLocation` | hit, maps（フォルダ → 対応 のキャッシュ） | `@{Name; Folder; Rest; Known}` | 検索結果のインデックス名・元のフォルダ・その下の相対フォルダ。検索対象フォルダの対応、無ければ親フォルダのもの（インデックス名のフォルダを直接指定した場合）を使う。分からなければ Known = `$false`・Folder = 空（Name は返すため、フォルダを選んでもらえば設定に記録できる） | 同上 | resolveSourcePath, 画面 |
| `joinSourcePath` | folder, rest, name | string | フォルダ・相対フォルダ・ファイル名をつなぐ（ドライブ直下でも `\` を重ねない） | – | 画面 |
| `findMovedSource` | picked, rest, book | `@{Path; Root}` / `$null` | 選んだフォルダの中から元のファイルを探す。選んだフォルダを元のフォルダ、その下のフォルダ…の順に当てはめて試し、見つかったファイルと、元のフォルダに当たるフォルダ（Root）を返す | [03_画面_2_検索タブ_1 4.5.2](03_画面_2_検索タブ_1_元のファイルを開く.md#452-元のファイルが見つからないとき元のフォルダを設定する) | 画面 |
| `resolveSourcePath` | hit, maps | string / `$null` | 検索結果の元のファイルのパス（ファイルがあるかは確かめない）。元のフォルダが分からなければ `$null` | [03_画面_2_検索タブ.md](03_画面_2_検索タブ.md) | 画面 |
| `readSearchHistory` / `addSearchHistory` | path / word, path, max | string[] | 検索履歴の読み込み・追加（同じワードは先頭に移す） | – | 画面 |
| `readSearchOption` / `writeSearchOption` | path（既定 `$settingsFile`） / option（`@{UseRegex; CaseSensitive; FileFilter}` のうち変える項目）, path | `@{UseRegex; CaseSensitive; FileFilter}` / – | 画面の検索条件（`setting.config` の `useRegex` / `caseSensitive` / `fileFilter`。無ければオフ・空）。保存は option にある項目だけを変える | – | 画面 |
| `getConversionState` | since, path | `@{Exists; Folders; Total; Pending; Failed; Done; ConvertedSince; Updated; FailedRows}` | 変換一覧の状態ごとの件数と、失敗したファイルの行（FailedRows。変換日時の新しい順） | [03_画面_4_状態と操作の流れ.md](03_画面_4_状態と操作の流れ.md) | 画面 |
| `getOfficeProcesses` | – | プロセス情報の配列 | 実行中の Excel・Word・PowerPoint（Id・ProcessName・AppName・Background・StartTime・MemoryMB・Title）。`MainWindowHandle` が 0 ならバックグラウンド | [03_画面_3_Office強制終了タブ.md](03_画面_3_Office強制終了タブ.md) | 画面 |
| `stopOfficeProcesses` | ids | `@{Id; Stopped; Message}` の配列 | `Stop-Process -Force` で終了し、成否と理由を返す | [03_画面_3_Office強制終了タブ.md](03_画面_3_Office強制終了タブ.md) | 画面 |
