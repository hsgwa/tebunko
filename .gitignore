# ユーザーごとの設定
setting.config

# 自動生成（インデックス・途中状態・検索結果）
work/

# 手元の動作確認用データ（大きいファイルを含むため、リポジトリには入れない）
sample*/

# 作業用の git worktree（CLAUDE.md「作業場所」。マージしたら削除する）
.claude/worktrees/

# 開発ツールの個人設定（共有する設定 .claude/settings.json は追跡する）
.claude/settings.local.json

# 手元のメモ（やり残しの TODO）
/TODO.md
