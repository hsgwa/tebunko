tebunko_grep を起動できませんでした。

この PC では、PowerShell のスクリプトの実行が止められています（上に出ているエラーが原因です）。
多くの場合、組織のグループポリシーで実行ポリシーが決められています（下の一覧の MachinePolicy か UserPolicy に値があります）。
その値が AllSigned か Restricted のときは、tebunko_grep は起動できません。tebunko_grep は組織の設定を回避しません。

管理者に、ツールのフォルダのスクリプトの実行を許可するよう依頼してください。
この画面に出ている内容を伝えると、原因を調べやすくなります。

実行ポリシーの一覧:
