<!-- インデックスの新規作成・編集ダイアログ（［1 インデックス管理］タブ）。gui.ps1 が読み込む。
     色・文字・コントロールの形は scripts/shared/xaml/theme.xaml にまとめてある。 -->
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="インデックス" Width="620" Height="380" MinWidth="520" MinHeight="340"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="CanResize"
        FontFamily="Yu Gothic UI, Meiryo UI" FontSize="13"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType"
        Background="White" UseLayoutRounding="True" SnapsToDevicePixels="True">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../../shared/xaml/theme.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>

  <DockPanel Margin="18,16,18,16">
    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
      <Button x:Name="OkButton" Style="{StaticResource Primary}" Content="OK" IsDefault="True" MinWidth="104" Height="32" />
      <Button x:Name="CancelButton" Content="キャンセル" IsCancel="True" MinWidth="104" Height="32" Margin="8,0,0,0" />
    </StackPanel>

    <StackPanel>
      <TextBlock x:Name="IntroText" Style="{StaticResource Meta}" Margin="0,0,0,6" />

      <TextBlock Style="{StaticResource Label}" HorizontalAlignment="Left" Margin="0,12,0,6"
                 Text="元のフォルダ（Office ファイルのある場所）" />
      <DockPanel>
        <Button x:Name="BrowseButton" DockPanel.Dock="Right" Content="参照…" MinWidth="80" Margin="8,0,0,0" />
        <TextBox x:Name="FolderBox" AllowDrop="True" />
      </DockPanel>
      <TextBlock x:Name="FolderHint" Style="{StaticResource Micro}" Margin="2,6,0,0"
                 Text="パスの入力・貼り付け・フォルダのドラッグ＆ドロップで指定できます。" />

      <TextBlock Style="{StaticResource Label}" HorizontalAlignment="Left" Margin="0,14,0,6" Text="インデックス名" />
      <TextBox x:Name="NameBox" />
      <TextBlock x:Name="NameHint" Style="{StaticResource Micro}" Margin="2,6,0,0"
                 Text="検索対象の一覧と検索結果に、この名前が出ます。\ / : * ? &quot; &lt; &gt; | は使えません。" />

      <TextBlock x:Name="NoticeText" Style="{StaticResource WarnText}" Margin="0,14,0,0" Visibility="Collapsed" />
      <TextBlock x:Name="ErrorText" Style="{StaticResource ErrorText}" Margin="0,10,0,0" Visibility="Collapsed" />
    </StackPanel>
  </DockPanel>
</Window>
